#!/usr/bin/env sh
# .husky/pre-commit
# Rulează lint-staged + verificare TypeScript înainte de commit.

# Lint-staged formatează și verifică doar fișierele staged
npx lint-staged

# Type-check rapid pe tot (incremental)
# Comentează această linie dacă devine prea lent (>10s).
npx tsc --noEmit
