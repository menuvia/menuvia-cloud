# CLAUDE.md — reguli de lucru în repo-ul Menuvia

Citește întâi `ARCHITECTURE.md` (harta sistemului în 1 pagină).

## Reguli nenegociabile

1. **Regula de aur:** bani + bon fiscal = Plan 3 (`pro`/`enterprise`), fără excepții.
   Orice feature care atinge plăți/bon/TVA se gate-uiește **în RPC/RLS**, nu doar în UI.
2. **Migrațiile aplicate nu se editează.** Schimbările = fișier nou în
   `supabase/migrations/`. CI-ul („Apply all migrations") le rulează pe Postgres
   efemer cu asserțiuni pe fluxul de comandă — trebuie să treacă înainte de push.
3. **Planul aparține restaurantului, nu user-ului.** Pentru gating în UI folosește
   `useFeatures(restaurantId)` → `planTier()` din `src/lib/features.ts`,
   NU `profile.plan` (greșit pentru staff).
4. **Numele comerciale** (Meniu Digital / Meniu + Comenzi / Fiscalizare) doar în UI;
   intern și în DB rămân `free/starter/growth/pro/enterprise`.

## Convenții cod

- TypeScript strict, **fără `any`**. Stiluri inline cu tokens din `D` (`lib/constants.ts`) — nu există Tailwind.
- Comentarii și UI în română; cod (identificatori) în engleză.
- ESLint `--max-warnings 0` e blocking; prettier `format:check` e advisory.
- Tab-urile mari din dashboard sunt lazy-loaded — păstrează pattern-ul.

## Verificare înainte de push

`npm run typecheck && npm run lint && npm run test` (build-ul cere env vars placeholder — vezi `.github/workflows/test.yml`).
Jobul Playwright E2E din ci.yml e ERMETIC (Supabase local + seed, zero secrets) și verde pe main din august 2026 — un E2E roșu e o regresie reală până la proba contrarie (mențiunea veche „cronic roșu, secrets lipsă" era stale; audit v3 CA-04).
**Overlay-urile `role="dialog"` fixate jos (cookie banner, cardurile PWA de instalare/actualizare) interceptează click-urile pe bara de navigare mobilă** — E2E-ul le pre-setează în `prepPage` (e2e/helpers.ts: consent + `pwa-install-dismissed` + `pwa-update-snoozed`) ÎNAINTE de prima navigare; cardul de instalare are timer de 30 s, deci fără pre-setare un test pica în funcție de CÂT dura (03-dashboard-nav „Facturi", mobile-safari, sept 2026). `test.afterEach(dumpOverlaysOnFailure)` scrie în log textul dialogurilor deschise la eșec — Playwright spune doar „intercepts pointer events", iar artefactele nu sunt accesibile din sesiunea agentului. Cardul de actualizare SW are „Mai târziu" (`PWA_UPDATE_SNOOZE_KEY`, amânare pe SESIUNEA de navigare — sessionStorage: supraviețuiește reload-ului în același tab, dispare la închiderea tab-ului/PWA-ului; la o deschidere REALĂ SW-ul din `waiting` nu mai are clienți vechi și se activează singur, deci actualizarea se aplică FĂRĂ card — cardul reapare doar dacă alt tab ține SW-ul vechi în viață; NU scrie „reapare la următoarea deschidere") — înainte NU avea nicio ieșire în afară de reload. **PayModal (recenzie adversarială #246)**: suma înmânată are O SINGURĂ sursă (`handed`, folosită și de buton — `parseFloat(amount) || grandTotal` lăsa `-5`/`1e400` să plece la server în timp ce preflight-ul tăcea), un input ne-valid dezactivează butonul (validare de input, nu gate de business), iar preflight-ul de supra-încasare NU promite conversia automată în bacșiș (serverul o respinge cu `overpayment`, mig 264) — spune că plata va fi respinsă și oferă „Trece X lei la bacșiș" (`tipsMode='custom'`). Testele PayModal aserțează pe fragmentul UNIC al fiecărei ramuri (`/bacșiș/i` se potrivea pe ambele).

## Capcane cunoscute

- `advance_order` există în mig 085 ȘI 087 (copie sincronizată + gate) — modifici întâi 085, oglindești în 087. Lanțul de redefiniri ulterioare (FĂRĂ twin): 118→147→172→214→243→262→263→264→**270** — orice recreare păstrează TOATE invariantele (guard `cancel_over_payments` în ramura `cancel` — mig 270, anularea peste `order_payments` ne-gol e respinsă, autoritatea e trigger-ul `trg_orders_cancel_ledger_gate` în DATE; lock `for update of o`, cancel_reason_required, overpayment pe AMBELE ramuri, gate-ul `fiscal_plan_requires_payment` pe `close_order` (mig 263: pe planurile cu `fiscal_receipt` NU există închidere nefiscală; mig 264 l-a coborât și în DATE, prin `trg_orders_closed_fiscal_gate`), plafon de încasare SUS **și JOS** (`overpayment` + `underpayment`, mig 264 — o sumă sub total ar lăsa comanda `paid` cu bon imposibil de emis), `paid_amount_required` (mig 264, rundă review — `mark_paid` FĂRĂ sumă și fără plăți parțiale era acceptat și trecea comanda în `paid` cu registrul de plăți GOL; pe ramura CU parțiale restul se deduce din registru și NULL rămâne legitim), `v_final - v_tips` → `v_net` (mig 262: bacșișul NU intră NICIODATĂ în `order_payments`/`paid_amount` — doar în `orders.tips_amount`; `p_paid_amount` rămâne suma înmânată CU bacșiș, contractul PayModal), gate fiscal, gate metodă `invalid_payment_method` — `card_online` NU e înregistrabil manual de staff, paritate cu `add_partial_payment` 231). Teste LH1–LH6: `tests/sql/low_hardening_assertions.sql` (+ anti-leak slug post-lanț și revoke-ul anon pe `check_reservation_rate_limit` din mig 243).
- **Două gate-uri pe regula de aur coborâte în DATE (mig 270, audit v3 RES-25/RES-31)**, teste CL1–CL7 (+CL7s, clichet structural pe RPC-ul compus) `tests/sql/cancel_ledger_gate_assertions.sql` + RR1–RR8 `tests/sql/receipt_retry_ambiguous_assertions.sql`, toate mutațiile verificate că PICĂ pe codul vechi. (A) **„Anulează" peste bani încasați e respins**: `add_partial_payment` (258) și `settle_table_payment` split (229) depun bani reali în `order_payments` pe comenzi NE-terminale (`served` cu 60/100; `preparing` cu 40 lei `card_online` pe Stripe), iar ramura `cancel` verifica doar rol + motiv → bani în sertar/Stripe fără bon, scoși din rapoarte (`v_order_payment_methods` exclude `cancelled`) dar numărați de `cash_collected_for_shift`. Trigger `trg_orders_cancel_ledger_gate` (BEFORE UPDATE, oglinda lui `trg_orders_closed_fiscal_gate` din 264 — închide și PATCH-ul direct sub `orders: admin all`) + același guard în `advance_order` (mesaj cu suma ÎNAINTE de scriere). Gate-ul e pe REGISTRU, nu pe status/motiv (CL3). Singurul scriitor viu de `cancelled` pe `orders` e `advance_order`. Client: `advanceOrderStatus` aruncă `Error` real cu `hint`/`code` (ca `createOrder`), `useOrders.advance` are `opts.throwOnError`, `CancelOrderDialog` e TRISTATE pe `paidSoFar` (sumă cunoscută >0 → buton dezactivat; `null` = necunoscut → activ, serverul decide) și afișează mesajul serverului prin `describeCancelRejection`. **Ieșirea din gate e `void_order_payment(uuid, text)`** (echipa roșie: fără ea, un split online pe o comandă `preparing` pe care bucătăria n-o poate onora bloca masa PERMANENT — cancel respins, `close_order` respins pe Plan 3, `mark_paid` = bon fals, `close_session_orders` refuză cât timp comanda e ne-terminală): DEFINER `public, pg_temp`, doar owner/manager (`is_admin`, waiter → `role_insufficient`), motiv obligatoriu (`void_reason_required`), DOAR comenzi ne-terminale (`order_terminal` — pe `paid` registrul e sursa bonului), șterge rândul și scrie `audit_log` DELETE cu `old_data` = rândul + `void_reason` (CL6 — mutația „fără audit" pică). Semantica: banii au fost RETURNAȚI (cash înapoi / refund MANUAL în Stripe — nu se face refund automat). Dialogul oferă „Stornează plățile și anulează" cu motiv obligatoriu, iar clientul cheamă UN SINGUR RPC, `void_order_payments_and_cancel(uuid, text)` (recenzie CodeRabbit pe #246: N storno-uri + un cancel ca N+1 cereri lăsau registrul stornat parțial sau comanda deschisă cu registrul gol la un eșec la mijloc) — o singură tranzacție, compune `void_order_payment` (audit per plată) + `advance_order` (toate invariantele lanțului), CL7 verifică că refuzurile (waiter / fără motiv) lasă registrul INTACT. Ordinea guard-urilor e înghețată (served fără motiv → `cancel_reason_required` ÎNAINTE de registru, CL1). Testul HH0 cere `tgtype = 5` EXACT (un `after insert or update` = 21 ar re-evalua reducerea la orice UPDATE), iar HH4b verifică scope-ul de categorie FĂRĂ cap (cu cap, 12.00 și 14.50 ajung amândouă la 5.00 și masca aplicarea regulii pe toată comanda). (B) **Retry-ul bonului AMBIGUU cere ack server-side**: `bridge_retry_receipt` are lanț 030→038→262→**270** cu semnătură NOUĂ `(uuid, boolean default false)` — DROP + CREATE obligatoriu (un `create or replace` ar lăsa vechea semnătură → PGRST203 la orice apel, ca `register_affiliate` 243; asserție „exact 1 semnătură"); gate pe MARKER `like 'POSIBIL DUPLICAT%'` — PREFIX, paritate exactă cu `enqueue_invoice_for_order` (262); ambele surse (cron `bridge_mark_stale_as_error` și bridge `fiscalnet.js`) îl pun la început, iar error_code diferă, deci markerul e singurul contract — independent de status (cancel NU șterge `error_info`, RR6), hint `ambiguous_receipt` fără `p_ack_ambiguous=true`. Retry-ul CU ack lasă URMĂ în `error_info` („Retrimis după verificarea benzii (ack admin <uid> la <data>)", RR3) — `bridge_confirm_receipt` o suprascrie la următorul rezultat. Calea cu urmă pentru un bon agățat în `sent`: `bridge_force_resolve_stuck(id, false)` (045) scrie „Force-resolved … NOT printed" → fără marker → retry fără ack trece (RR8). Backstop în DATE: `trg_pending_receipts_block_client_repend` (BEFORE UPDATE OF status) respinge →`pending` din `anon`/`authenticated` (aveau UPDATE pe tabel sub `admin manage`, 030); funcția e deliberat NE-definer (trebuie să vadă rolul APELANTULUI — promovarea la DEFINER face RR5 vacuu, asserție anti). BridgeTab trimite `p_ack_ambiguous: true` DOAR după confirmDialog. Skew fail-closed: client vechi + DB nouă → ambiguul e blocat; client nou + DB veche → PGRST202 doar pe rândurile ambigue. Rezidual consemnat, NEadresat: `authenticated` are și INSERT pe `pending_receipts` (030) — nu se poate bloca pe rol fiindcă enqueue-ul 259 rulează ca authenticated pe INSERT-direct-paid.
- `PLAN_NAMES` (features) e DERIVAT din `PLAN_LABELS` (constants) + prefix emoji (refactor aug 2026) — sursa unică e `PLAN_LABELS`; redenumirea unui plan se face DOAR acolo. NU reintroduce o tabelă separată în features.ts.
- Comanda QR cere sesiune de masă (mig 088): `qr` fără `session_id` = respins server-side.
- **Authorization lockdown (mig 096A/B/C).** Mutațiile pe `public.restaurant_memberships` sunt gate-uite de `trg_enforce_owner_membership_invariant` (CONSTRAINT TRIGGER DEFERRABLE INITIALLY DEFERRED). Fires la COMMIT și respinge orice stare finală fără exact 1 owner membership aliniat cu `restaurants.owner_id`. Tranzitul intra-tx e permis; corectitudinea la COMMIT e obligatorie.
- **owner_id e imuabil.** Modificarea `restaurants.owner_id` e blocată de `trg_restaurants_owner_id_immutable` (BEFORE UPDATE pe rând). Singura cale autorizată: `scripts/apply_ownership_remediation.sql` — DISABLE/ENABLE per-trigger sub lock strict, fără `session_replication_role`.
- **slug routing.** Schimbarea `restaurants.slug` trece exclusiv prin RPC-ul `change_restaurant_slug` (UPDATE column-level pe `slug` e revocat). `RESTAURANT_UPDATE_FIELDS` (în `src/lib/sanitize.ts`) NU mai conține `slug` — `useRestaurants.update()` filtrează tăcut payload-uri care încearcă să-l trimită.
- **7 RPC-uri SECURITY DEFINER pe acest lockdown**: `preview_invite`, `accept_invite`, `create_restaurant`, `change_member_role`, `remove_member`, `revoke_invite`, `change_restaurant_slug`. Toate returnează `jsonb`, au `search_path = public, pg_temp`, PUBLIC zero EXECUTE. Orice RPC nou pe această suprafață trebuie să respecte aceeași convenție.
- **Funelul de autorizare `is_admin`/`is_member`/`my_role`** (mig 096a) a fost extins EXACT de două ori: `or is_platform_admin()` (mig 186) și `or has_partner_access()` (mig 187). Orice recreare viitoare a trio-ului trebuie să păstreze AMBELE escape-uri (mig 187 are asserție anti-regresie) — pierderea lor rupe founder dashboard-ul și accesul partenerilor peste tot.
- **`has_partner_access` cere atribuire ne-terminală** (mig 193): `canceled/refunded/expired` NU dau acces. Filtrul e oglindit în `list_partner_restaurants` — dacă modifici unul, modifică-le pe ambele, altfel panoul afiliatului listează restaurante pe care RLS-ul le respinge.
- **Afilierea e cu CERERE + aprobare** (mig 224): `register_affiliate` (lanț 097d→188→224→**243** (+handler unique_violation anti dublu-submit, ca `create_restaurant` 221), semnătura NOUĂ `(text,text,text)` — cea veche e DROP-uită, anti overload PostgREST) creează afiliați `pending` cu telefon obligatoriu; fondatorul decide prin `admin_review_affiliate`. Gate-urile pe `status='active'` (atribuire 097c, comision 099, parent, partner access) fac pending/rejected INERȚI fără alt cod — NU slăbi aceste filtre. `admin_list_affiliates` are lanț 186→188→224 (+phone/application_note/reviewed_at). Default-ul coloanei `status` rămâne `'active'` intenționat (seed-urile din tests/sql se bazează pe el).
- **`update_order_items` are lanț lung de redefiniri** (079→080→081→082→146→184→192) fără twin de sincronizat; `create_order` are ultima definiție în mig **191** (copie verbatim a lui 145 + guard grup) — orice recreare pornește din 191, NU din 145. Ambele au guard de minim per grup cu hint `missing_required_group`, acoperit permanent de `tests/sql/order_group_min_assertions.sql` în CI.
- **`affiliate_payouts.wise_transfer_id` e BIGINT** (mig 098), iar parametrul RPC-urilor e text — conversia se face validat în corp (mig 193). `coalesce(text_param, bigint_col)` direct în UPDATE nici măcar nu se planează: RPC-ul moare la ORICE apel (bug reparat o dată; nu-l reintroduce).
- **Comisioanele afiliaților se citesc LIVE** din `affiliates.*_bps` la fiecare `invoice.paid` (mig 099) — editarea din FounderPage are efect imediat. Defaults-urile stau în `platform_settings` (mig 188, founder-only); public se expun DOAR setup/recurring/cap prin `get_affiliate_public_defaults` (mig 189) — niciodată `cascade_bps`.
- **Mod founder/partener (frontend)**: cheile `menuvia_founder_view` + `menuvia_founder_view_from` (localStorage) prin `lib/founder.ts`. RestaurantContext injectează membership sintetic 'manager' doar dacă SELECT-ul RLS trece; erorile Supabase vin în `{error}` (supabase-js NU aruncă) — nu trata `data:null` de la un blip de rețea drept „fără drepturi" (curăță cheia doar pe răspuns valid cu zero rânduri).
- **Funcțiile noi primesc `search_path` din start** — mig 194 a reparat retroactiv 14 funcții vechi; advisor-ul `function_search_path_mutable` nu trebuie să mai crească.
- **Platform admins self-healing** (mig 195): trigger `trg_seed_platform_admin` pe `profiles` setează `is_platform_admin=true` pentru emailurile fondatoare (georgeradu119 + georgeradu004) la INSERT/UPDATE — repară capcana din mig 168 (seed pierdut dacă contul nu exista încă). Lista de emailuri e în `fn_seed_platform_admin`.
- **`create_reservation_public` are lanț de redefiniri** (151→199→201→**241**), FĂRĂ twin de sincronizat, dar orice recreare TREBUIE să păstreze TOATE: 10 argumente cu `p_table_id uuid` (mig 199, branch race-safe sub `pg_advisory_xact_lock`, hint `table_unavailable`), plafonul de durată `least(...reservation_duration)` (mig 151), gate-ul `is_module_enabled('reservations')`, logica wrap-around pentru program peste miezul nopții (mig 201: fereastra validă când `close_time <= open_time` e `[open,24:00) ∪ [00:00,close)`), și **ziua de SERVICIU** (mig 241): pe wrap-around, slotul din `[00:00, close)` se verifică pe open_days-ul zilei PRECEDENTE (`extract(isodow from ... - interval '1 day')`) — altfel toate sloturile post-miezul-nopții ale ultimei zile de program erau fals respinse. Mig 201/241 au asserții fail-closed. Teste RS1–RS3: `tests/sql/reservation_service_day_assertions.sql`.
- **RPC-urile publice de rezervare-cu-hartă** (`get_public_floor_plan`, `get_tables_availability`, mig 199; availability are lanț 199→200→**246** — predicat sargabil `starts_at < ends AND ends_at > starts` în loc de OVERLAPS + guard NULL; asserție anti-reintroducere operator) sunt gate-uite de modul (mig 200: null / `raise module_disabled` dacă `reservations` e OFF — default OFF). SECURITY DEFINER, `search_path=public,pg_temp`, grant anon+authenticated. Client-side, `fetchTablesAvailability` (qr.ts) ARUNCĂ pe eroare (nu `[]`) — `ReservationSheet` se bazează pe asta ca `.catch`-ul să seteze `mapError`.
- **Meniu multilingv** (mig 197): `products.translations`/`categories.translations` (jsonb) + `restaurants.menu_languages` (jsonb). `restaurants` e column-gated → coloana nouă a cerut `grant update (menu_languages) ... to authenticated` (ca la slug). `RESTAURANT_UPDATE_FIELDS` (sanitize.ts) + testul JS + whitelist-ul SQL F6 din `tests/sql/authorization_final_state_assertions.sql` trebuie sincronizate (4 locuri).
- **i18n meniu = `T(lang, key)` pe `PUBLIC_MENU_STRINGS`** (lib/publicMenuStrings.ts — extras din constants.ts ca tabela să NU intre în entry chunk; fără re-export din constants): suportă acum 7 limbi (ro/en/de/fr/it/hu/es) — ORICE cheie nouă TREBUIE să aibă TOATE cele 7 câmpuri (altfel `T` întoarce `undefined` la runtime). Limbă nesuportată → fallback EN. `availableMenuLangs(categories, allowed?)` intersectează cu `restaurant.menu_languages` doar când lista e ne-vidă (fără regresie pe localuri neconfigurate). Chrome-ul urmează limba ALEASĂ (`menuLang`) când e non-RO, nu `restaurant.language`.
- **Idempotența comenzii QR se rotește pe SUCCES** (`QrMenuPage.tsx`, `rotateIdempotencyKey` după `createOrder` reușit), nu doar la reset — altfel un refresh/back pe mobil retrimite o comandă nouă cu cheia veche → dedup server → confirmare veche, venit pierdut tăcut. `createOrder` (orders.ts) aruncă un `Error` real (nu obiectul Supabase brut) ca hint-urile de business (`missing_required_group`) să ajungă la client.
- **`useRestaurants.load()` (useData.ts) nu colapsează la listă goală pe eroare**: dacă interogarea `owned` eșuează → setează eroare, nu `[]` (altfel un owner ajunge fals la Onboarding pe un blip de rețea). Reminderele de rezervare (send-reservation-reminders.js) resetează `reminder_sent_at=NULL` pe eșec de enqueue (altfel se pierd permanent). Emailurile Resend folosesc `Idempotency-Key` din `email.id` (dedup real vs. reclaim mig 167).
- **ai-proxy — base_url `custom` se re-validează la fetch** (assertSafeBaseUrl în `callOpenAI`, imediat înainte de fetch, fără await intermediar) + redirect blocking; reziduu documentat: pinning IP complet cere `undici` (nu e dependență). `oblio-generator` derivă `vatName` din procent (19/21→Normala, 9/5/11→Redusa, 0→SFDD) — NU hardcoda 'Normala'.
- **Panou „Stadiu mese"** (`TableStatusBoard.tsx` + `TableStatusMap.tsx`, comutabil în `WaiterPage`, gate `paymentsEnabled = planTier ≥ 3`): agregă LIVE din comenzile deschise (`useOrders`, realtime) + `waiterCalls`, FĂRĂ migrație/RPC nou. Ocuparea se derivă pe `table_id` (acoperă QR + ospătar uniform), nu pe `table_sessions` (care e populat doar de QR). Modul „Hartă" refolosește `floor_layout` + `resolveTableId`/canvas din `lib/floorPlan` (aceleași coordonate ca editorul), colorat pe 5 stări de staff cu tokenii D (NU paleta publică PUB ca `FloorPlanViewer`). Cardul de masă e `BoardTableCard`, refolosit de grilă ȘI de detaliul de sub hartă. „+ Adaugă la masă" trece prin `WaiterEntry` cu prop `initialTableId` (rundă nouă pe aceeași masă). `WaiterPage` fetch-uiește `restaurants.floor_layout` (`maybeSingle`, cast validat cu `Array.isArray(floors)`).
- **Meniul QR pe un singur RTT** (`get_menu_for_restaurant`, lanț mig 212→246→**253** — toate agregările sunt liniare: `group_options` per grup (246), `prod_groups_json`/extras/pairings per produs + `cat_products` per categorie (253, ieșire BYTE-identică, verificat empiric); orice recreare păstrează forma jsonb identică cu qr.ts + filtrele anon): SECURITY DEFINER, grant anon+authenticated (meniul e public), asamblează tot arborele `Category[]` server-side. `fetchMenuForRestaurant` (qr.ts) îl cheamă ÎNTÂI și cade pe implementarea pe straturi (`fetchMenuLayered`) dacă RPC-ul lipsește (frontend înaintea migrației) — fallback-ul NU se șterge. Mig 245 adaugă COMPUSELE `resolve_qr_menu` + `get_menu_by_slug` (1 RTT pe scanare QR și /m/:slug): compunere PURĂ a RPC-urilor de bază (zero proiecții proprii — whitelist-urile 217/219 + filtrele 212 se moștenesc; asserție anti citire-directă-de-tabele), client `resolveQrMenu`/`fetchMenuBySlug` (qr.ts) cu fallback pe fluxul în doi pași — nici acestea nu se șterg. Test LH7. DEFINER ocolește RLS, deci filtrele de vizibilitate anon sunt scrise EXPLICIT în RPC (restaurant activ, produse `is_active AND NOT is_draft`, opțiuni/extras `is_available`) și trebuie ținute în sincron cu politicile din mig 153/140/023/008. Forma jsonb TREBUIE să rămână identică cu tipurile din qr.ts. Teste: `tests/sql/menu_rpc_assertions.sql` (MR1–MR5, incl. anti cross-tenant).
- **Proiecția publică pe slug NU expune secrete** (`get_restaurant_by_slug`, mig 148→217): grant anon, suprafața `/m/:slug` indexabilă. `wifi_password` și `qr_token` sunt întoarse ca `null::text` (mig 217) — meniul public afișează doar badge-ul „are wifi" din `amenities`, nu parola; `resolve_qr_token` (mig 206) nu le-a expus niciodată. `menu_languages` E în proiecție (mig 219, DROP+CREATE) → whitelist-ul de limbi se aplică pe /m/:slug (ca pe QR). Orice recreare TREBUIE să NU reintroducă `r.wifi_password`/`r.qr_token` în SELECT (mig 217/219 au asserție fail-closed) ȘI să păstreze `r.menu_languages`. Aceeași disciplină de whitelist ca `resolve_qr_token`/`get_menu_for_restaurant`.
- **Bucla de dunning e completă** (stripe-webhook.js + `process_lifecycle_events` mig 180→216→220): eșec → `payment_failed` (email „⚠️ Plata nu s-a putut procesa" + grace past_due, downgrade doar pe terminal) → recuperare → `payment_recovered` (email „✅ Plata s-a procesat"). Webhook-ul emite `payment_recovered` DOAR când `invoice.attempt_count > 1` (recuperare reală), altfel un email de „succes" la fiecare ciclu recurent. `invoice_paid` (mig 216) ȘI `plan_changed` (mig 220) au ramuri NO-OP explicite în RPC (evenimente de audit/afiliere tratate în webhook) — fără ele cădeau în `else` scriind `unknown_event_type` pe fiecare factură/schimbare de plan. `payment_recovered` e deja în enum `email_template_kind` (mig 039). CTA-ul emailurilor („Actualizează metoda de plată →", `/dashboard?tab=billing`) e REAL: `stripe-portal.js` (Billing Portal, service prin `lib/billing.ts`) + cardul „Abonament & facturare" pe Acasă (HomeTab, admin tier≥2). Founder trebuie să activeze o dată Customer portal în Stripe Dashboard.
- **Oblio: eșec AMBIGUU e TERMINAL, nu se re-face** (mig 218 `bridge_oblio_mark_ambiguous` + oblio-generator.js): timeout/rețea în POST → `status='failed'` fără requeue (cron-ul NU reia), fiindcă Oblio poate fi creat deja factura → retry-ul auto = DUPLICAT fiscal. Founderul retrimite MANUAL (`admin_retry_invoice`) după verificare. Erorile 4xx clare trec prin `bridge_oblio_mark_failed` (retry auto sigur). NU reintroduce requeue pe ramura ambiguă.
- **Oblio: `deliveryDate` e ziua LIVRĂRII, iar starea e-Factura se vede** (mig 269, rangul 14 audit v3), teste OB1–OB6 `tests/sql/oblio_delivery_date_assertions.sql`. Ambele goluri erau REALE dar LATENTE — tabela `invoices` e GOALĂ în producție (zero facturi emise vreodată), deci se plătesc la PRIMA factură reală. (1) **`composeOblioInvoice` punea `today` în toate cele trei date.** Data livrării determină EXIGIBILITATEA TVA, deci așază factura într-o perioadă fiscală; `bridge_oblio_get_queued` nici măcar nu întorcea `orders.paid_at`. Se rupe când emiterea și încasarea se despart: plată la 23:55 + cron la 00:05 (la 31 ale lunii → LUNA următoare), retry cu backoff, sau retrimitere MANUALĂ a fondatorului după verificare în Oblio (mig 218/239) — care poate fi la ZILE distanță. Lanț `bridge_oblio_get_queued` 041→181→239→**269** (+`order_paid_at`, coloană la FINAL); `issueDate` și `dueDate` NEATINSE (o scadență anterioară emiterii poate fi respinsă de Oblio). **Clientul formatează în Europe/Bucharest, nu UTC**: o plată la 00:30 EEST e 21:30 UTC în ziua PRECEDENTĂ, deci `toISOString().slice(0,10)` ar muta livrarea cu o zi înapoi exact la comenzile de noapte — verificat: `2026-09-04T21:30:00Z` dă `2026-09-04` pe UTC și `2026-09-05` pe Europe/Bucharest. Se folosește `Intl.DateTimeFormat('sv-SE', {timeZone:'Europe/Bucharest'})`, care produce nativ YYYY-MM-DD. (2) **`invoices.oblio_einvoice` se SCRIA din mig 041 dar nu-l citea nimeni**: `list_invoices_for_restaurant` nu-l proiecta, tipul `Invoice` nu-l avea, InvoicesTab nu afișa nimic — pe o factură B2B fondatorul nu putea ști dacă e-Factura a ajuns în SPV (obligație legală din 2024). Nu erau date lipsă, erau date capturate și aruncate la UI. Acum RPC-ul întoarce `has_einvoice boolean` (prezență, NU XML-ul — un blob n-are ce căuta într-o listă), iar badge-ul apare doar pe facturi EMISE și B2B; semnalul care contează e ABSENȚA. **AMBELE RPC-uri au cerut DROP+CREATE** (un `create or replace` nu poate schimba tipul de RETURN) — fiecare are UN singur consumator și coloana nouă e la FINAL, deci cast-ul din client nu se rupe. **Capcană de catalog**: coloanele unui `returns table` NU stau în `pg_type`/`pg_attribute` (`prorettype` e `record`, fără `typrelid` compozit) — sunt parametri OUT, în `proargnames` filtrat pe `proargmodes = 't'`; varianta greșită întoarce NULL. **Capcană de test**: `list_invoices_for_restaurant` are gate `is_admin`, care citește `auth.uid()`; suita rulează ca `postgres`, fără JWT → zero rânduri și un `select into` care dă NULL TĂCUT, indistinct de „fără e-Factura". OB4 setează `request.jwt.claim.sub` pe OWNER (`is_admin` verifică întâi `restaurants.owner_id`) ȘI are guard pe `count(*) = 1` înainte de a citi valoarea. Cele trei mutații (claim fără `order_paid_at`, `has_einvoice` mereu true, claim fără `for update skip locked`) pică toate. **Partea JS are teste proprii** (`tests/functions/oblio-generator.test.js`): suita Oblio existentă NU atingea deloc datele facturii — verifica `products`/`vatName`/`price`/`internalNote`, niciodată `issueDate`/`dueDate`/`deliveryDate` — deci un „Bridge tests verde” nu spunea NIMIC despre schimbarea centrală a migrației. Cele trei cazuri noi acoperă ziua încasării vs. ziua emiterii, cazul de FUS (`2026-09-04T21:30:00Z` = 00:30 EEST pe 5 septembrie → `2026-09-05`, nu `2026-09-04`) și fallback-ul pe `order_paid_at` NULL. Ambele mutații pică: revenirea la `deliveryDate = today` și formatarea în UTC.
- **`create_restaurant` are handler unique_violation pe slug** (mig 221): INSERT-ul e în buclă cu `exception when unique_violation` + sufix aleator (anti TOCTOU între pre-check `while` și insert). Orice recreare trebuie să păstreze handler-ul (altfel onboarding-ul eșuează cu 23505 brut pe cursă de slug).
- **Merge realtime comenzi doar pe tranziție de status** (useOrders.ts `isStatusTransition`): fast-path-ul de merge (fără refetch) se ia DOAR când total neschimbat ȘI un câmp de status/timestamp chiar s-a schimbat. O editare de produse cu total identic cade pe refetch — altfel rămân produse STALE pe Bucătărie/Ospătar. `isPlatformAdmin()` (lib/ai.ts) e TRISTATE: `null` = eroare de verificare, NU „interzis" — un blip nu mai scoate fondatorul din /founder (FounderPage arată „Reîncearcă"; DashboardPage tratează null ca non-admin doar pentru tab-ul AI).
- **Loyalty v1 (mig 226) acordă puncte la intrarea în ('paid','closed')**, NU doar la 'paid': pe growth (planul minim al loyalty-ului) comenzile nu ajung niciodată 'paid' — `enforce_paid_status_gate` cere fiscal_receipt (Plan 3); fluxul growth se termină în 'closed'. Indexul parțial unic pe `loyalty_events(order_id) where kind='earn'` garantează UN singur earn (pro trece prin paid→closed). Telefonul se stochează DOAR ca `fn_loyalty_phone_hash` (md5 pe număr normalizat RO: +40/0040→0) — nu compara hash-uri calculate altfel. Test permanent: `tests/sql/loyalty_assertions.sql`.
- **Tichetele de bucătărie (mig 227) sunt NEfiscale** — coadă separată `kitchen_tickets`, NU atinge `pending_receipts`/lanțul fiscal (mig 133). Enqueue = CONSTRAINT TRIGGER DEFERRED la COMMIT pe `orders` cu catch-all `raise warning` (un tichet eșuat NU avortează niciodată comanda). Gate plan `kitchen_tickets` = growth+; `bridge_register` are lanț 030→133→227 (gate lărgit `fiscal_receipt OR kitchen_tickets`, ACEEAȘI semnătură) și `enforce_bridge_device_fiscal_gate` (mig 149) e lărgit la fel — orice recreare le păstrează pe AMBELE. Retry-ul e INOFENSIV (hârtie dublă, nu bon dublu) — spre deosebire de fiscal/Oblio. Mutațiile pe tichete trec DOAR prin RPC-uri (bridge_* cu device_secret; retry/cancel is_admin; reprint is_member); `kitchen_tickets_mark_stale` = service_role, chemat orar din automation-cron (tolerant PGRST202 până se aplică migrația). Bridge-ul tipărește ESC/POS pe TCP 9100 sau file-drop (`bridge/lib/kitchenPrinter.js`, transliterare RO obligatorie — imprimantele termice nu au diacritice). Teste permanente KT1–KT9: `tests/sql/kitchen_ticket_assertions.sql`; CS6 (shifts) e rescris pe noul contract.
- **SMS-urile tranzacționale (mig 228) intră în coadă prin TRIGGERE, nu prin RPC-uri** — `trg_sms_reservation_confirmed` (reservations) + `trg_sms_pickup_ready` (orders, WHEN `ready`+`pickup`), AMBELE exception-wrapped (`raise warning`, nu rethrow): un eșec de SMS nu avortează NICIODATĂ rezervarea/advance_order (test SQ9). `enqueue_sms` face `return null` pe TOATE ramurile negative (modul OFF / plan free / plafon lunar atins / telefon ne-mobil-RO) — nu transforma skip-urile în raise. Modulul `sms_notifications` a lărgit whitelist-ul CHECK din `restaurant_modules` + `set_restaurant_module` (lanț 086→228, copie exactă + un rând); plafonul lunar = `plan_features.limit_value`, numărat în luna Europe/Bucharest, exclusiv `cancelled` (și eșecurile consumă — anti-abuz). Worker: `process-sms-queue.js` (cron `*/15` în regimul de avarie — vezi bullet-ul 254/255; SMSO.ro, template-uri FĂRĂ diacritice — un caracter non-GSM-7 înjumătățește segmentul la 70). SMSO nu are Idempotency-Key → dublu-send rezidual acceptat, logat „DUBLU-SEND RISC". Teste permanente SQ1–SQ10: `tests/sql/sms_queue_assertions.sql`.
- **Notificarea restaurantului la rezervare nouă (mig 254/255)**: trigger `trg_email_reservation_created` AFTER INSERT pe `reservations`, exception-wrapped (ca SMS-urile 228 — un eșec de enqueue NU avortează rezervarea), dedup `resv_created:<id>`, destinatar `profiles.email` al owner-ului (izvorul din mig 061), FĂRĂ gate de plan (semnal operațional, nu feature). Enum-ul `reservation_created` vine din mig 254 (fișier FĂRĂ tranzacție, ca 230/233). Template-ul e în process-email-queue.js — worker necunoscător de kind marchează `failed`, deci codul se deployează ÎNAINTEA migrației (fără rânduri de kind nou = inofensiv). Teste RN1–RN3: `tests/sql/reservation_owner_notify_assertions.sql`. **Anularea publică pe cod** (mig 256/257): `cancel_reservation_by_code(p_slug,p_code)` — DEFINER, grant anon, rate-limit `check_rate_limit` 15/15min pe slug, ANTI-ORACLE (slug greșit = cod greșit = `invalid_code`), doar pending/confirmed viitoare; emailul de anulare către owner (`reservation_cancelled`, enum mig 256 fără tranzacție) e în RPC (nu trigger — anularea din dashboard nu se auto-notifică), exception-wrapped. UI: `/rezervare/:slug?cancel=COD` + linkul din ecranul de succes. Teste RC1–RC4: `tests/sql/reservation_cancel_assertions.sql`. Cron-ul `process-sms-queue` e la `*/15 * * * *` DELIBERAT (regimul de avarie post-suspendare Netlify Free, aug 2026 — vezi bugetul din netlify.toml) — revine la minut la primul client cu SMS. ORICE schimbare de schedule în netlify.toml se oglindește AICI în același commit (documentația care minte pe cron-uri a re-declanșat o dată bugetul care a omorât cron-ul).
- **No-show rezervări (mig 233/234)**: `claim_reservation_reminders` are lanț 057→**215**→234 (215 = pasul de RECLAIM pe rândurile revendicate-dar-neprocesate, doar canalul email — NU-l pierde la recreare; review-ul E5 l-a prins pierdut o dată) — orice recreare păstrează reclaim-ul ȘI AMBELE canale (email non-null SAU mobil RO + modul `sms_notifications` + feature de plan; fără gate-uri, rezervările fără canal ar „arde" `reminder_sent_at` degeaba). În worker (send-reservation-reminders.js), `enqueue_sms`=null e SKIP definitiv (flag rămâne setat), doar EROAREA reală resetează flag-ul — un reset pe null ar re-revendica aceeași rezervare la nesfârșit. `auto_mark_reservation_no_show` (cron orar, service_role) atinge DOAR `confirmed` depășite de grație (podea 30 min) și DOAR în fereastra rulantă de 48h (anti-backfill: fără ea, primul tick ar fi rescris tot istoricul de confirmed nefinalizate în no-show-uri false): pending = decizie de staff, seated = clientul a venit. Cheia de recidivist = ultimele 9 cifre (`get_reservation_no_show_counts`, aceeași normalizare ca rate-limit mig 115/129) — NU hash-ul loyalty. Enum `sms_template_kind` += `reservation_reminder` (mig 233, fișier fără tranzacție ca 230). Teste NS1–NS5: `tests/sql/reservation_noshow_assertions.sql`.
- **MFA platform (mig 235)**: `is_platform_admin` are acum lanț 168→235 — cere `aal2` DOAR când `profiles.mfa_enforced` e true (fail-open pe ne-înrolați; `aal` citit din GUC `request.jwt.claims` prin `fn_session_aal`, NU `auth.jwt()` — absent din bootstrap-ul CI). Clichet anti-downgrade: `set_my_mfa_enforced(false)` cere sesiune aal2 (altfel un atacator doar-cu-parola ar stinge flag-ul). Funelul is_admin/is_member/my_role NU se atinge — enforcement-ul se propagă prin escape-ul existent; mig 235 + testul MF4 verifică că ambele escape-uri (186/187) au rămas. UI: MfaCard (SettingsTab→Cont, enroll TOTP), pas de cod în AuthPage post-parolă, ecran explicativ în FounderPage pe sesiune aal1 cu MFA pending. Teste MF1–MF4: `tests/sql/mfa_platform_assertions.sql`.
- **White-label v1 (mig 236)**: branding pe `affiliates` (brand_domain unic lowercase + name + logo), scris DOAR prin `admin_set_affiliate_branding` (founder, audit, domeniile Menuvia rezervate). `resolve_agency_branding` = RPC anon cu whitelist STRICT de 2 câmpuri și filtru `status='active'` (asserție fail-closed anti-leak) — aceeași disciplină ca `get_restaurant_by_slug`. `admin_list_affiliates` are lanț 186→188→224→236 — orice recreare păstrează TOATE câmpurile (cerere 224 + comisioane 188 + branding 236). Frontend: decizia e RUNTIME pe `window.location.hostname` (`lib/whiteLabel.ts`, Netlify servește același bundle pe orice domeniu; promisiune memoizată la nivel de modul); `MenuBrandBadge` cade tăcut pe badge-ul Menuvia la orice eroare. Pași CNAME: `docs/WHITE_LABEL.md`.
- **Benchmark founder (mig 237)**: `admin_monthly_benchmark` interoghează DIRECT `orders`, NU `v_daily_orders` — view-ul are gate-ul fiscal în WHERE și ar ascunde complet restaurantele non-fiscal; comenzile se compară pe toate planurile, dar câmpurile de BANI sunt NULL fără `fiscal_receipt` (asserție pe `has_fiscal` în corp) și mediile de venit se calculează doar pe fiscal. Luna = Europe/Bucharest; comenzi pe created_at, venit pe paid_at (ca ReportsTab). Teste WB1–WB5: `tests/sql/white_label_benchmark_assertions.sql`.
- **Stripe Connect deauthorized (Q4)**: `stripe-connect-webhook.js` tratează acum `account.application.deauthorized` (poartă `account`, nu payment_intent — ramură separată înaintea fluxului de intent): golește `stripe_account_id` prin `set_restaurant_stripe_account(id, null)` (mig 204 acceptă null) ca localul să poată reconecta după ce a revocat platforma din Stripe. Fără el, `status`/`link` picau permanent și `begin_table_payment` folosea un cont mort.
- **Securitate Q4 (mig 240)**: `trg_enforce_order_table_tenant` (mig 113) se declanșează acum pe `before insert OR update` — un UPDATE direct al `orders.table_id` la masa altui restaurant era scăpat (trigger doar pe INSERT). `bridge_devices` NU mai are politica `members read` (expunea `device_secret` oricărui waiter/kitchen prin PostgREST): adminii păstrează accesul prin `admin manage` (FOR ALL), cititorii SQL sunt DEFINER. AI quota TOCTOU (apeluri paralele) e ACCEPTAT ca reziduu: `ai_record_usage` (mig 171) clampează cu `least(greatest(...))` sub `FOR UPDATE` → soldul nu poate deveni negativ; overshoot-ul de cost sub concurență e inerent oricărui check-then-act cu apel extern. Teste SQ1–SQ3: `tests/sql/security_q4_assertions.sql`.
- **Defalcarea pe metodă vine din REGISTRU, nu din enum** (mig 267, rang 10 audit v3), teste PM1–PM10 `tests/sql/payments_by_method_assertions.sql`. `orders.payment_method` e UN SINGUR enum per comandă: la orice split (parțiale 017/111/231, split pe itemi 229, online + rest cash) `advance_order` scrie `'other'`, deci TOȚI banii cădeau în „Alte metode". Măsurat pe producție ÎNAINTE de fix: 88 lei clasificați greșit (restaurant 1: cash 1317→1355, card 0→30, alte 78.50→10.50; restaurant 2: alte 20→0, cash 0→9, card 0→11). Sursa unică e **`v_order_payment_methods`** — o linie per (comandă, metodă), cu DOUĂ ramuri: A) `order_payments` grupat pe metodă; B) fallback pe `orders.payment_method`/`paid_amount` pentru comenzile FĂRĂ registru. **Ramura B NU e opțională**: 23 din 25 de comenzi plătite din producție nu au niciun rând în `order_payments` — un view care ar citi doar registrul ar raporta 88 lei în loc de 1415.50 (asserție pe `union all`). Deasupra ei: `v_daily_payments_by_method` (zi = ziua ÎNCASĂRII, `paid_at` în Europe/Bucharest, ca ReportsTab) și `v_daily_orders` (lanț 007→…→263→**267**), ale cărui cinci coloane de metodă se derivă acum din sursa unică — NUMELE și ORDINEA coloanelor sunt NEATINSE, deci AnalyticsTab (`select('*')`) s-a reparat fără nicio schimbare de frontend. Join-ul din `v_daily_orders` e NEcorelat (subquery grupat, nu `lateral`) — un lateral peste sursa unică ar re-evalua gate-ul fiscal și anti-join-ul ramurii B per comandă, exact tiparul per-rând scos în 253/263 (asserție anti-`lateral`). Invariantul care închide bucla: `cash+card+voucher+online+other == revenue` pe fiecare zi/restaurant (asserție pe datele reale în migrație, PM3 în teste) — garantat de mig 264. **Clichet pe taxonomie**: găleata `other` prinde tot ce nu e una din cele 4 metode cunoscute, deci o metodă A ȘASEA ar fi înghițită tăcut (regresia CA-02/MF-12 care a cerut o dată `online_revenue`) — asserția pe `enum_range(payment_method)` **ȘI pe CHECK-ul de pe `order_payments.method`** (ramura A bucketează pe el, nu pe enum) pică și forțează sweep-ul pe TOATE defalcările. Clichetul stă în DOUĂ locuri deliberat: în migrație (o singură evaluare, la poziția 267) ȘI ca **PM10** în suita permanentă — acolo rulează pe starea FINALĂ a lanțului la fiecare rulare de CI, deci o migrație VIITOARE care lărgește taxonomia îl declanșează; doar în migrație ar fi fost un gate care nu mai rulează niciodată. ReportsTab citește view-ul cu FALLBACK pe bucketarea veche când lipsește (frontend înaintea migrației) — fallback-ul NU se șterge. Bacșișul nu apare nicăieri (mig 262 l-a scos din `paid_amount` ȘI din `order_payments`).
  **Două capcane găsite de echipa roșie ÎNAINTE de merge, ambele reproduse pe replay — nu le reintroduce.** (1) **Politica de citire a lui `order_payments` TREBUIE să treacă prin funel.** `order_payments: member read` (mig 017) inline-a un `exists` pe `restaurant_memberships`, deci NU avea escape-urile `is_platform_admin()` (186) / `has_partner_access()` (187) pe care le are `orders: members read` prin `is_member()`. Sub `security_invoker`, fondatorul în founder-view (membership SINTETICĂ, fără rând în tabel) și partenerul vedeau comenzile dar ZERO plăți → ramura A goală, anti-join-ul ramurii B adevărat pentru TOT → view-ul cădea înapoi pe enum. Măsurat: owner/waiter `47 cash / 41 card`, fondator/partener `0 / 0 / 88 alte`. Totalurile reconciliau (88 = 88) în AMBELE cazuri, deci nicio asserție de reconciliere nu putea prinde clasa — fondatorul ar fi văzut fix cifra greșită pe singura suprafață unde auditează numerele clienților. Mig 267 rescrie politica pe `is_member`, `to authenticated` (anon n-are SELECT pe tabel, deci clasa 264 nu se declanșează). **PM8 e singura asertie din suită care rulează SUB RLS** — restul rulează ca `postgres`, care ocolește RLS și e ORB la clasa asta. (2) **Ramura B folosește `coalesce(paid_amount, total)`, nu `paid_amount is not null`.** O comandă `paid` cu `paid_amount` NULL (istoric pre-264; producibilă și azi printr-un PATCH direct sub `orders: admin all`) nu apărea în NICIUNA dintre ramuri, dar ReportsTab o număra în venit (`paid_amount ?? total`) — deci găleţile încetau să închidă cu titlul de deasupra lor, o REGRESIE față de bucketarea client-side veche, care partiționa exact aceeași valoare. De aceea `v_daily_orders.revenue` se calculează acum din ACELEAȘI găleți: reconcilierea devine TAUTOLOGIE, nu o asserție care poate pica. ReportsTab pliază în plus reziduul în „Alte metode". Ambele teste (PM8/PM9) au fost verificate că PIC pe codul dinainte de fix — o asertie care trece și cu bug-ul prezent e mai rea decât una lipsă. (3) **NEcorelat nu e suficient — join-ul TREBUIE să poarte `restaurant_id`.** Fără el subquery-ul nu expune nicio coloană pe care planificatorul să împingă filtrul exterior, deci agregă TOT istoricul TUTUROR restaurantelor fiscale înainte de hash join. Verificat pe cluster propriu (30k comenzi / 10 restaurante): partea internă sare de la 3.000 la 30.006 rânduri agregate, 11,6 ms → 50,5 ms; la 100k comenzi / 50 de restaurante echipa roșie a măsurat 86 ms → 1369 ms pe owner și 83 ms → **6,6 s pe calea FONDATORULUI** (nelimitată de RLS prin `is_platform_admin`, deci prima care cedează). Cu RLS ocolit complet, 9,3 ms → 228 ms: ~24× vine din FORMA join-ului, nu din costul funelului. Amplificarea crește cu datele din AFARA restaurantului interogat, deci e invizibilă la volumul de azi. Asserție anti-regresie pe `pm.restaurant_id` în definiția view-ului. O a doua echipă a măsurat independent, pe 100k comenzi / 8 tenanți, **28,4 s pe calea fondatorului** (875 ms înainte) — peste `statement_timeout`-ul implicit de 8 s al rolului `authenticated`, deci AnalyticsTab n-ar fi fost „lent", ci ar fi ARUNCAT. (4) **Scutirea de la gate-ul fiscal a lui `v_daily_payments_by_method` e verificată, nu presupusă.** View-ul moștenește gate-ul din sursa unică, deci asserția nu-l caută literal — dar atunci TREBUIE verificat că sursa chiar e sursa, altfel e un gate mort. Dovedit prin MUTAȚIE: un view rescris pe `orders`+`order_payments` direct, cu cifre identice dar fără semi-join fiscal, trecea și asserțiile migrației, și toată suita PM. PM6 era în plus VACUU pentru view-ul zilnic (filtrează `status='paid'`, iar un local growth nu POATE avea comenzi `paid` — mig 124 aruncă la INSERT), deci „growth are 0 rânduri" trecea și fără gate. Acum: asserție de DERIVARE în migrație (`v_order_payment_methods` prezent în definiție) + PM6 testează scenariul de **DOWNGRADE** (pro → comandă `paid` → growth), singurul în care gate-ul face muncă reală — banii istorici ai unui local care nu mai e pe Plan 3 nu au voie să apară în niciunul dintre cele trei view-uri.
- **Defalcarea zilnică pe interval trece prin RPC, nu prin view** (mig 268, reziduul de performanță al lui 267), teste DP1–DP7 `tests/sql/daily_payments_rpc_assertions.sql`. `v_daily_payments_by_method` grupează pe o EXPRESIE (`date_trunc('day', paid_at at time zone 'Europe/Bucharest')::date`), iar un filtru pe acea cheie calculată NU e sargabil: planificatorul nu poate folosi niciun index pe `paid_at`, deci agregă TOT istoricul restaurantului și abia apoi taie fereastra — indexul construit exact pentru asta, `orders (restaurant_id, paid_at) WHERE status='paid'` (mig 244), rămâne nefolosit. Măsurat pe cluster propriu (80.053 comenzi / 8 restaurante / 400 de zile, fereastră de 30 de zile pe UN restaurant): forma veche `Bitmap Index Scan on orders_restaurant_created_idx → actual rows=10000` (tot istoricul), 10,159 ms; forma sargabilă `orders_paid_at_idx → actual rows=600` (exact fereastra), 1,244 ms. Factorul (8,2×) nu e miezul — PANTA e: forma veche crește cu istoricul TOTAL, cea nouă cu fereastra CERUTĂ. Pe producție AMBELE dau Seq Scan (53 de comenzi, tabel prea mic pentru orice index), deci un EXPLAIN pe prod ar fi „dovedit" fals că nu e nimic — măsurarea se face pe date seed. `get_daily_payments_by_method(uuid, date, date)` e **SECURITY INVOKER deliberat** (ca `get_database_size`, mig 266): RLS-ul ȘI gate-ul fiscal vin MOȘTENITE din `v_order_payment_methods`; un DEFINER le-ar ocoli și m-ar obliga să re-implementez gate-ul aici — exact tiparul care a produs gate-ul MORT din 267. View-ul RĂMÂNE (interogări ad-hoc, consumatori fără interval). Interval jumătate-deschis la dreapta (`< p_to + 1 zi`), ca ultima secundă a ultimei zile să intre. **DP6 e clichetul structural, VIU**: aceleași verificări există în migrație, dar acolo se evaluează o singură dată, la poziția 268 din lanț. Verificat prin MUTAȚIE: un RPC rescris să delege view-ului (deci NEsargabil, adică fără niciun motiv să existe) și unul promovat la DEFINER treceau AMBELE de DP1–DP5, fiindcă REZULTATELE rămân identice — DEFINER se vede doar sub RLS, iar suita rulează ca `postgres`, care o ocolește. Un test care verifică doar cifrele e ORB la clasa asta.
- **`vat_report_daily` aplică reducerea de comandă** (lanț 028→029→031→125→150→238→**253** — gate fiscal ca SEMI-JOIN, nu per-rând): view-ul scalează fiecare linie cu factorul `orders.total / subtotal` (post-discount), EXACT ca `oblio-generator.js` — altfel raportul TVA SUPRADECLARA brut+TVA față de bon/factură la orice comandă cu discount (Happy Hour mig 077). `orders.total` e întreținut post-discount de trigger-ul `order_items_subtotal_sync` (mig 031) din `discount_type/discount_value` — la teste NU seta `discount_amount` direct (trigger-ul îl suprascrie), folosește `discount_type='amount'`. Orice recreare păstrează factorul + gate-ul fiscal (mig 150) + `security_invoker` (mig 125).
- **Facturi Oblio blocate în 'generating' (mig 239)**: un kill de proces între claim (`bridge_oblio_get_queued`, lanț 041→181→239 — stampilează `generating_since`) și emitere lasă factura agățată permanent. `oblio_reclaim_stale_generating` (service_role, cron orar Job 1e, tolerant PGRST202) o marchează `failed` cu eroare AMBIGUĂ după `p_minutes` (fallback pe `created_at` pentru rândurile pre-239) — NU re-pune în coadă (mig 218: kill în POST = ambiguu, retry MANUAL după verificare în Oblio). Apoi apare în `admin_list_invoice_failures`. Teste FQ1–FQ3: `tests/sql/fiscal_q4_assertions.sql`.
- **Subtotalul comenzii se sincronizează PER-STATEMENT** (mig 248): trigger-ul `order_items_subtotal_sync` (mig 003, era FOR EACH ROW) a fost înlocuit cu TREI triggere FOR EACH STATEMENT (`order_items_subtotal_sync_ins/upd/del`) cu transition tables, care cheamă `recalc_order_subtotal_stmt` → `_refresh_order_totals` (mig 031, NEATINS) O SINGURĂ dată per comandă atinsă per statement (nu N ori). Totalul final e IDENTIC (recompute complet idempotent) — doar amplificarea (N UPDATE-uri pe orders → N rânduri audit_log mig 044 + N evenimente realtime mig 078) a dispărut. Orice recreare păstrează cele 3 triggere statement-level (asertație pe `tgtype & 1 = 0`) + funcția `recalc_order_subtotal` veche DROP-uită. Happy-hour (mig 077) neafectat. Teste SS1–SS5: `tests/sql/subtotal_stmt_assertions.sql`.
- **`affiliates` SELECT e OWN-ROW** (mig 249): politica „affiliate: read own" a fost îngustată de la `affiliate_visible_ids()` (own + downline) la `profile_id = auth.uid()` — mig 224 adăugase `phone`/`application_note` (PII) și un afiliat-părinte putea recolta prin PostgREST brut datele de contact ale downline-ului. Frontend-ul NU citește raw din affiliates (arborele vine din `get_affiliate_dashboard` DEFINER; founderul din `admin_list_affiliates` DEFINER), deci îngustarea nu strică nimic. Finalizează mig 103/104/190 pe tabelele-soră; asertație anti-regresie (fără `affiliate_visible_ids`, cu `profile_id`).
- **Stoc: deducere pe ('paid','closed')** (mig 250, oglindă loyalty 226): `deduct_stock_on_order_paid` (mig 026) scădea DOAR la →'paid', dar comenzile growth se termină în 'closed' (gate fiscal mig 124), deci stocul nu scădea niciodată pe growth. Acum gate-ul e `NEW.status in ('paid','closed')` + skip pe `OLD.status in ('paid','closed')` (anti dublă-scădere pe pro new→paid→closed). Trigger-ul rămâne `after update of status`. **Idempotența e DB-enforced** (mig 252, oglindă loyalty 226): tabela-claim `order_stock_deductions(order_id pk)` + `insert ... on conflict do nothing` la începutul funcției → deducere EXACT o dată per comandă, indiferent de re-intrări în starea terminală (ex. owner care face `closed→served→closed` prin UPDATE direct — permis de `orders: admin all`). Fără backstop, se dubla stocul. Mig 252 face și **backfill** pentru comenzile deja `paid`/`closed` la momentul aplicării — altfel garanția ar fi acoperit doar comenzile viitoare, lăsând tot istoricul vulnerabil la re-intrare. Guardurile de status (mig 250) rămân ca fast-path. Teste SD1–SD4: `tests/sql/stock_deduction_assertions.sql`.
- **`receive_purchase_order` cost mediu robust pe stoc negativ** (mig 251, lanț 026→160→251): WAC-ul `(current_stock*cost + qty*price)/(current_stock+qty)` iese NEGATIV când `current_stock < 0` (frecvent după mig 250 pe supra-vânzare growth) și numitorul rămâne negativ → încalcă CHECK-ul `cost_per_unit >= 0` (mig 026) → RECEPȚIA ÎNTREAGĂ eșua (NIR blocat). Fix: pe `current_stock <= 0` costul devine prețul livrării (`unit_price`), nu WAC. Orice recreare păstrează lock-ul `for update` (mig 160) ȘI guardul `when current_stock <= 0 then` (asserție fail-closed pe ambele). Teste PC1–PC3: `tests/sql/purchase_order_cost_assertions.sql`.
- **Happy Hour SE aplică SERVER-SIDE automat** (`trg_apply_happy_hour_auto`, mig 077, constraint trigger DEFERRED la COMMIT; teste permanente HH0–HH7 `tests/sql/happy_hour_auto_apply_assertions.sql` — audit v3 RES-34, invariantul de bani n-avea niciun test care să-l EXERSEZE; capcane de test: `set constraints all immediate` se pune DUPĂ `create_order`, apoi `set constraints all deferred` înaintea următoarei comenzi — altfel trigger-ul trage pe INSERT-ul gol, subtotal 0, fără reducere; rate-limit-ul QR per masă (090) respinge a 3-a comandă în 2 min pe același token, iar `now()` e înghețat per tranzacție → fiecare comandă QR de test are masa/token-ul ei; regula se face mereu activă cu `'00:00:00'`–`'24:00:00'` și `days_of_week='{}'`): pentru comenzile `qr`/`pickup` calculează cea mai bună regulă activă (scope all/category/product + max_discount) și o scrie ca `discount_amount` pe comandă — clientul PLĂTEȘTE prețul redus chiar dacă UI-ul intermediar arată prețul întreg. Card + ProductSheet primesc `happyHourPct` DOAR pentru afișare (paritate vizuală); NU replica math-ul reducerii client-side (scope/cap-urile fac suma exactă imposibil de calculat local). Nu re-flag-ui „HH e doar cosmetic" — banii sunt corecți.
- **Toolchain-ul de build/lint (31 aug 2026): Node 22, eslint 10 flat, vite 8, vitest 4.** Lint-ul e `eslint.config.js` (flat; `.eslintrc.cjs` e ȘTERS) cu paritate 1:1 față de vechea configurație: doar `*.ts/*.tsx`, eslint:recommended prin import DIRECT din `@eslint/js` (devDependency explicită din sept 2026 — înainte era o sondare `createRequire` care cădea tăcut pe `{}` fiindcă pachetul lipsea din lockfile: regulile core NU rulau în CI, audit v3 CA-01), iar **react-hooks e fixat EXPLICIT la `rules-of-hooks` + `exhaustive-deps`** — preset-ul v7 aduce suita React Compiler (`set-state-in-effect` & co., 87 de semnalări pe pattern-uri deliberate); adoptarea ei = refactor separat, NU o strecura la un bump. `manualChunks` din vite.config.ts e FUNCȚIE (rolldown-ul din vite 8 nu mai acceptă obiectul). Node 22 e pinuit în 9 locuri (ci/test/deploy-vps/regenerate-lockfile + netlify.toml + setup-vps.sh) — un bump viitor le atinge pe TOATE.
- **Dependabot: capcane operaționale dovedite (31 aug 2026).** (1) REFRESH-ul unui PR de grup existent păstrează membership-ul vechi — ordinea grupurilor nu se aplică decât la regenerare; de aceea `minor-and-patch` are `exclude-patterns` EXPLICITE pe familiile de toolchain (eslint*/ts-eslint/vite*/vitest*) — nu te baza pe ordine. (2) Dependabot NU mai rebazează un branch pe care a împins altcineva — după un fix împins de agent, conflictele cu main sunt ale tale; lockfile-ul NU se rezolvă manual: fluxul e `package.json` merged de mână (chei disjuncte) + lockfile-ul din main ca placeholder + workflow-ul `regenerate-lockfile` (regenerare DE LA ZERO pe runner — reconcilierea incrementală contra unui lockfile stale pică pe ERESOLVE). (3) Push-urile cu GITHUB_TOKEN nu declanșează CI, iar dispatch-ul de workflow e 403 pentru agent — de aceea workflow-ul are și trigger pe push `regen-lock/**`; comentariile agentului cu `@dependabot <cmd>` sunt NEUTRALIZATE de relay (mențiunea se sparge cu puncte mediane) — nu funcționează.
- **Poarta de securitate NU mai folosește `npm audit` — sursa e OSV.dev** (`scripts/osv-audit-gate.mjs` + biblioteca `scripts/lib/osv-audit.mjs`, teste offline `tests/osv-gate/`). Motivul retragerii: arborele de PRODUCȚIE conține un alias (`web-vitals-soft-navs: npm:web-vitals@6.2.1`, din posthog-js), npm nu poate exprima aliasurile în `security/advisories/bulk` și cade pe legacy `security/audits/quick`, pe care registry-ul îl retrage — a picat ~18h continuu pe 3–4 sept 2026, blocând ORICE PR. Jobul `security-audit` nu mai rulează deloc `npm ci`: citește doar `package-lock.json`, deci nu mai atinge registry-ul npm.
  **Ce trebuie să supraviețuiască oricărei rescrieri** (fiecare punct e un fals-verde demonstrat empiric de echipa roșie pe trei propuneri independente):
  1. **CONTROL POZITIV (`CANARIES`) — cel mai important.** OSV răspunde `{}` pentru „fără vulnerabilități”, deci „am evaluat 166 de pachete și n-am găsit nimic” e byte-cu-byte identic cu „am trimis 166 de nume pe care OSV nu le-a auzit niciodată” sau „backend degradat”. Toate verificările de formă trec și poarta iese 0 fără să fi evaluat nimic — CA-01 renăscut. De aceea în ACELAȘI lot se interoghează pachete cunoscut vulnerabile; dacă nu se întorc, e `Fatal`. NU șterge controlul; dacă un canar e retras de OSV, ÎNLOCUIEȘTE-l.
  2. **`querybatch` NU întoarce severitate** — doar `{id, modified}`. Severitatea cere pasul doi, `/v1/vulns/{id}`. A citi severitatea din querybatch dă `undefined`, iar `undefined >= 'high'` e `false` = critical trecut tăcut.
  3. **Severitatea e MAXIMUL** dintre bucket-ul GHSA și cel derivat din vectorul CVSS, nu primul găsit: un aviz etichetat MODERATE cu vector 9.8 e CRITICAL. `severity[].score` e VECTOR STRING, nu număr — `parseFloat` dă NaN și `NaN >= 7` e `false`. GHSA scrie **MODERATE**, nu MEDIUM. `--audit-level=high` INCLUDE critical. Rotunjirea e cea din CVSS v3.1 Appendix A (aritmetică pe întregi), NU `ceil(x*10)/10` — aia e v3.0 și diferă exact la pragurile 7.0/9.0.
  4. **Numele real al pachetului = `entry.name` din lockfile**, scris de npm DOAR la aliasuri (o singură intrare aici); altfel segmentul de după ULTIMUL `node_modules/` (`lastIndexOf`, nu `indexOf` — altfel intrările imbricate ies `posthog-js/node_modules/fflate`). Interogarea după numele din cale ar da zero vulnerabilități pe veci — un punct orb care arată exact ca „curat”. NU folosi `npm ls`: raportează aliasul sub numele folderului și nu emite niciodată numele real.
  5. **Filtrul e `dev !== true`**, NU `!dev && !devOptional`: `npm audit --omit=dev` PĂSTREAZĂ `devOptional` (3 intrări aici). Nodurile `optional: true` non-dev (14) rămân și ele — `npm ci` le instalează și ajung în bundle. Dedup pe `name@version`, nu pe nume (`fflate` există la 0.8.3 ȘI 0.4.9).
  6. **Orice formă necunoscută de răspuns e TRANSPORT, nu „curat”**: element `null`, `vulns` ne-array, vulnerabilitate fără `id`, `results.length` nepotrivit, `next_page_token` prezent. Varianta permisivă (`r && Array.isArray(r.vulns) ? r.vulns : []`) transformă gunoiul în verde.
  7. **Fără guard `import.meta.url === argv[1]`** — pică tăcut cu exit 0 pe symlink/realpath/`node --import`. CLI-ul rulează necondiționat, iar verdictul stă în CODUL DE IEȘIRE, nu pe stdout. **Fără `OSV_API_BASE` din env**: o variabilă de repo ar redirecta oracolul spre un server care zice „curat” la orice.
  8. Avizele cu `withdrawn` se ignoră (alarmele false antrenează echipa să ocolească poarta). Un aviz din care NU putem calcula severitatea (ex. doar CVSS v4, care cere un tabel MacroVector, nu o formulă) e `Fatal`, nu „sub prag”. HTTP 400 = bug de-al nostru, eșec imediat; 429/5xx = transport, se reîncearcă.
  Poarta stă în JOBUL PROPRIU `security-audit`, nu în `lint-and-build`: `e2e` și `lighthouse` au `needs: lint-and-build`, deci acolo o cădere externă ȘTERGEA semnalul E2E (verificat — Playwright a ieșit „skipped”). NU o muta înapoi. Și NU o transforma niciodată în `|| true`.
- **`npm ci` din workflow-uri are `--no-audit --no-fund`**: auditul implicit nu blochează nimic (npm nu eșuează instalarea pe vulnerabilități, doar tipărește) dar trecea prin același endpoint retras și a ținut pasul „Install dependencies” agățat 13 minute.
- **Asserțiile „de eră" pot fi gate-uri MOARTE.** Suita `authorization_final_state_assertions.sql` rulează în CI sub `hashFiles(096b) != '' AND hashFiles(096c) == ''` — condiție permanent falsă de când există 096c, deci F1–F9 nu au mai rulat din iunie 2026. Whitelist-ul de coloane UPDATE pe `restaurants` (unul dintre cele 4 locuri care trebuie ținute în sincron) a fost extras ca `tests/sql/restaurant_update_whitelist_assertions.sql` (RW1) și e legat NECONDIȚIONAT în sql-verify.yml. Când adaugi o suită nouă, întreabă-te dacă `if:`-ul ei poate deveni fals pentru totdeauna — un gate care nu rulează e mai rău decât unul lipsă, fiindcă pare acoperire.
- **`supabase/` NU mai are copii legacy de migrații.** Cele 44 de fișiere `supabase/migration-0NN-*.sql` erau duplicate STALE ale primelor migrații (ex. 001 folosea `gen_random_bytes` neschematizat, în timp ce `supabase/migrations/` are `extensions.gen_random_bytes`) și au fost șterse în sept 2026. Sursa unică aplicată a fost dintotdeauna `supabase/migrations/`. `DEPLOYMENT_GUIDE.md` le lista ca procedură de deploy (8 fișiere din 264) — documentație care MINTE, exact tiparul care a omorât o dată cron-ul; e rescrisă pe `supabase db push`.
- **Prețul anual NU există** (audit v3, rangul 11). PricingPage avea un toggle „Anual −17%" pur cosmetic: `yearly` nu ajungea niciodată la `onCheckout` (semnătura e `(plan: string) => void`), iar `stripe-checkout.js` are un singur price ID per plan — clientul vedea „Facturat anual, 208 lei/lună" și era abonat LUNAR la 249. Toggle-ul și câmpul `priceYearly` sunt scoase; testul din `features.test.ts` păzește acum ABSENȚA câmpului. Reintroducerea facturării anuale cere, în ordine: prețuri anuale în Stripe, env var nou per plan (atenție la `.every(Boolean)` din stripe-checkout.js, devin obligatorii la boot), un parametru de perioadă dus prin `onCheckout` → `stripe-checkout.js`, ȘI intrările în `PLAN_BY_PRICE` din `stripe-webhook.js` — altfel webhook-ul nu recunoaște abonamentul anual.
- **Banner „Fiscalizare activă, casa nu e conectată" (mig 265 + client, audit v3 rangul 8)**, teste BC1–BC5 `tests/sql/bridge_connection_assertions.sql`. Pe Plan 3 comenzile se încasează normal chiar cu bridge-ul căzut, dar atunci NU se emite niciun bon — tăcut, exact în ziua 1–14 a fiecărui client nou. (1) **`bridge_devices.status` e un flag LIPICIOS — nu-l folosi ca liveness.** Toate scrierile îl duc pe `online` (`bridge_heartbeat` 030, `bridge_validate_device` 045, `bridge_get_pending`/`_tickets` 247/227) și NIMIC nu-l readuce pe `offline`: singura funcție care ar fi făcut-o, `bridge_devices_mark_stale` (mig 035), scrie într-o coloană `is_active` care nu există pe tabel, deci e moartă. Liveness-ul real se citește DOAR din `last_seen_at` (prag 3 min = 6 bătăi ratate la heartbeat-ul de 30 s). (2) **RPC nou, nu `select` din client**: mig 240 a șters politica „bridge_devices: members read" (expunea `device_secret` oricărui waiter), deci o interogare directă întoarce ZERO rânduri pentru staff și ar produce un „nu e conectată" FALS. `bridge_connection_status` (DEFINER, gate `is_member`, `search_path=public,pg_temp`) întoarce EXACT 3 chei — `registered`/`connected`/`last_seen_at` — și nimic secret; asserția BC5 îngheață forma, iar mig 265 verifică și că politica de citire pentru membri NU a reapărut (SQ3 din 240 o cere). (3) **Banner-ul e TRISTATE** (`BridgeOfflineBanner` + `useBridgeConnected`): se randează DOAR pe `connected:false` CUNOSCUT. O eroare de rețea sau un RPC nedeployat lasă starea `null` → niciun banner. Un fals pozitiv de alarmă fiscală e mai scump decât lipsa alertei: antrenează staff-ul să ignore exact ecranul care contează.
- **Alarma de stocare (mig 266 + /health, audit v3 rangul 12)**, teste DB1–DB4 `tests/sql/database_size_probe_assertions.sql`. Când Postgres atinge plafonul planului, baza trece în READ-ONLY: platforma nu mai acceptă comenzi, la NICIUN restaurant. E o cădere totală care se anunță cu săptămâni înainte și pe care nimeni nu o vede, fiindcă nimic nu o măsura — exact tiparul incidentului de cron din august, de unde și locul: `/health`, lovit din AFARĂ de UptimeRobot, nu un watchdog care trăiește în interiorul lucrului monitorizat. `get_database_size()` e **SECURITY INVOKER deliberat**, nu DEFINER: `service_role` are deja EXECUTE pe `pg_database_size` și CONNECT pe bază (verificat), deci DEFINER ar fi escaladare inutilă — asserția din migrație pică dacă cineva o „promovează”. Grant EXCLUSIV `service_role`; numele tabelelor și dimensiunile lor nu au ce căuta pe suprafața anon/authenticated. Praguri: 80% doar raportează (200, vizibil în payload), 90% dă 503, adică alertează; plafonul e configurabil prin `DB_SIZE_LIMIT_BYTES` (default 500 MB, planul Free) fiindcă planul se poate schimba fără redeploy. Alarma cară diagnosticul cu ea (primele 5 tabele) — fără asta founderul află că baza e la 92% și nu știe de unde să taie. Verificarea e tolerantă ca și cea de cron: eșecul ei lasă `unknown` și NU influențează codul de status, deci clientul se poate deploya înaintea migrației (PGRST202) fără alarmă falsă.
  **Trei corecturi din recenzia pe #240 (aplicate în follow-up), teste HL1–HL7 `tests/functions/health.test.js`.** (1) **Plafonul NU poate fi anulat din env.** `Number(env) || fallback` accepta `Infinity` ȘI valorile negative (ambele truthy): verificat empiric, `"Infinity"` → pct 0, `"-1"` → pct −2229365100, deci pragul de 90% devenea INACCESIBIL și alarma se stingea TĂCUT dintr-un typo — exact CA-01. Acum se acceptă doar valori finite ȘI strict pozitive. (2) **`/health` e PUBLIC**, deci `storage_detail` nu mai cară nume de tabele, octeți sau plafon: public rămân `checks.storage` + `used_pct` (severitatea, de care monitorul are nevoie), iar diagnosticul complet cere `HEALTH_DIAG_TOKEN` **EXCLUSIV prin antetul `x-health-diag`**, **FAIL-CLOSED** — env nesetat înseamnă zero detaliu, nu detaliu liber. `?diag=` e RESPINS deliberat (CWE-598: un secret în URL ajunge în logurile de request, în configul monitorului și în istoricul de shell). Prima variantă îl ACCEPTA, cu un comentariu în cod care descria exact riscul și numea antetul „calea intenționată” — a identifica o vulnerabilitate, a o descrie în scris și a o livra oricum e mai rău decât a nu o observa. Ce păzește asta e o ASERȚIE că un token CORECT trimis în query string e respins, nu o notă în comentariu. Dimensiunea lui `orders`/`audit_log` scurge volumul de business, nu doar schema. (3) DB4 verifica doar „ne-gol"; acum are și plafonul de 5 (ștergerea lui `limit 5` trecea nedetectată cât timp rândurile rămâneau ordonate). **Toate cele trei au fost prinse de CodeRabbit DUPĂ ce făcusem merge pe #240** — interogarea de review-uri întorsese gol cu un minut înainte ca recenzia să apară; înainte de orice merge se re-citesc review-urile, nu se are încredere într-un poll anterior. Rundă de întărire adversarială peste fix-urile de mai sus (CodeRabbit era din nou rate-limited, deci recenzia a fost proprie): (a) **comparația tokenului e `safeEqual`**, aceeași primitivă constant-time ca `x-cron-key` din `deploy/server.js` (SEC-09) — varianta „lungime + `===`" era ceremonie, iar comentariul care sugera protecție la timing era „documentație care minte"; (b) **PUBLIC = SEVERITATE, cu token = CIFRE**: nici `used_pct` nu mai e public, fiindcă plafonul implicit e o constantă publică (500 MB), deci un procent la 0,1% dă dimensiunea bazei la ±262 kB și, interogat zilnic, curba de creștere; (c) antetul `x-health-diag` e calea INTENȚIONATĂ (`?diag=` pune secretul în URL → loguri Netlify, config de monitor, istoric de shell); ramura `X-Health-Diag` era cod MORT (și Netlify, și shim-ul VPS trimit cheile minuscule). **Patru mutații care treceau înainte și pică acum**: ștergerea egalității din gate (rămânea „orice șir de lungimea bună"), un câmp nou scurs public (forma se îngheață cu `storage_detail === null`, nu se verifică pe câmpuri știute — disciplina BC5), dispariția preavizului de 80% (singurul avertisment din timp) și ștergerea căii prin antet. **`storage:'unknown'` e acum vizibil DIN AFARĂ** (`health-watch.yml` avertizează pe `db=ok` + `storage=unknown`): toleranța la RPC lipsă e corectă pentru deploy-înaintea-migrației, dar după aplicare `unknown` înseamnă „sondă moartă", iar codul nu poate distinge — clasa gate-ului mort, fix pe alarma pe care o întărește PR-ul. `HEALTH_DIAG_TOKEN` + `DB_SIZE_LIMIT_BYTES` sunt documentate în `.env.example`, `docs/VPS_RUNBOOK.md` (blocul `/etc/menuvia/env`), antetul lui `health.js` și `docs/RUNBOOK.md` §4.1 — fail-closed + nedescoperibil ar fi însemnat că diagnosticul nu există: exact precedentul „lista veche omitea STRIPE_WEBHOOK_SECRET" din runbook.
  **Ce NU s-a făcut, deliberat, și de ce.** Planul consiliului cerea și subțierea `audit_log`: pe UPDATE-urile de pe `orders` să se stocheze doar cheile schimbate. Măsurat pe PRODUCȚIE înainte de a scrie cod: baza are 21 MB (4,25% din plafon), `audit_log` are 539 de rânduri și a crescut cu 14 în ultimele 7 zile (0 în ultimele 24h). Mecanismul semnalat e real — `orders UPDATE` e tipul dominant de rând, cu ~1704 octeți de payload (`old_data` + `new_data` complete), iar la ținta de 500 comenzi/zi proiecția consiliului se verifică — dar la volumul de AZI e la ani distanță. Iar subțierea înseamnă pierderea instantaneului complet al rândului dintr-un jurnal FISCAL: nu mai poți reconstitui starea comenzii la momentul T. Asta e o decizie despre ce trebuie să poată DOVEDI auditul, nu una de performanță, și se ia cu fondatorul. Coloana `changed_keys` există deja și e populată pe UPDATE-uri, deci implementarea e ieftină CÂND se decide. Alarma de mai sus e exact lucrul care spune când.
- **Suitele de BANI au primit stratul de IDENTITATE (audit v3, PR lot B)**: `tests/sql/table_payment_assertions.sql` rulează ca `postgres` (superuser: ocolește ACL-ul de EXECUTE ȘI RLS), deci TP1–TP22 testează LOGICA RPC-urilor DEFINER, nu identitatea — cele 21 de suite care setează doar `request.jwt.claim.sub` rămânând superuser NU sunt „sub RLS", alimentează doar `auth.uid()`. TP3/TP8/TP14/TP20 apelează RPC-urile sub rolul REAL `service_role` prin `set_config('role','service_role',true)` … `set_config('role','none',true)` (în CI service_role n-are grant-uri pe tabele → `reset` ÎNAINTE de orice SELECT de verificare; `set_config` dintr-un sub-bloc `begin…exception` se rulează înapoi cu subtranzacția — pune-l în afara lui). **TP23 = clichet pe CATALOG** (`has_function_privilege` pe cele 6 semnături × anon/authenticated false, service_role true — fail-closed: un DROP+CREATE cu semnătură nouă face testul să ARUNCE „function does not exist" și lista se actualizează în același PR) + un apel real ca anon cu `sqlerrm like '%for function%'` (un test doar pe SQLSTATE e VACUU: `permission denied for schema` e tot 42501). **TP24 = citire sub RLS** cu 4 identități (owner DA, waiter NU, alt owner NU, fondator fără membership DA — escape-ul mig 186; moștenește fail-open-ul MFA din 235, ca PM8). Mutații verificate: revoke service_role → TP3 pică; grant authenticated → TP23 pică; `using(true)` pe politică → TP24 pică. Vitest (RES-24): cheia PICKUP P1–P8 (`orders-idempotency.test.ts` — fallback-ul e Map la nivel de MODUL, scope distinct per test), `PickupCheckoutSheet.test.tsx` S1–S4 (sloturile se aleg după regex `^\d{1,2}:\d{2}$`, nu după text — depinde de TZ-ul runner-ului; `buildPickupSlots` mock-uit cu ISO), `WaiterOrderCard.test.tsx` T1–T5 (tristate; **default-ul `paymentsEnabled` e acum `null`, fail-closed** — un call-site care omite prop-ul nu are voie să arate „Plată integrală" pe growth).
- **Lot C client (audit v3 RES-36/RES-27/RES-16/RES-19A)**: (1) `OrderTracker` oprește polling-ul pe `paid` (TERMINAL = paid/cancelled/closed, paritate cu `order_terminal` din `advance_order`; `clearInterval` în callback-ul intervalului, nu în `poll()` — poll rulează prima dată înainte să existe intervalul; `served` RĂMÂNE poll-uit: Plan 2 → closed, Plan 3 → paid). Vechea justificare „paid rămâne polling pentru bon" era FALSĂ: `fiscal_receipt_requested_at` e scris doar de acțiunea clientului. Test `OrderTracker.test.tsx` cu fake timers. (2) **Listele de staff au plafon PROPRIU**: `STAFF_ORDERS_FETCH_LIMIT = 500` — fără `.limit()`, PostgREST trunchia TĂCUT la `max_rows` (1000 hosted) și cu ASC păstra cele mai VECHI comenzi; acum cele mai NOI N (DESC + LIMIT N+1, întoarse ASC/FIFO) + flag `truncated` din `useOrders`, afișat de WaiterPage/KitchenPage. N+1 trebuie să rămână SUB `max_rows` (configurabil din Dashboard), altfel flag-ul devine el însuși mort. `fetchKitchenOrders`/`fetchWaiterOrders` întorc `StaffOrdersPage {orders, truncated}`. (3) **Snapshot-ul QR se RECONCILIAZĂ cu sesiunea confirmată de server** (`reconcileQrSessionSnapshot`, pur): `is_new` sau id diferit → comenzile/plățile rehidratate se ARUNCĂ (nota veche e închisă/plătită; altfel „Plătește masa" număra nota altcuiva). Banii rămân protejați server-side oricum. (4) **PayModal afișează refuzul serverului** (`describePayRejection`: underpayment/overpayment cu sumele, metodă, rol) ÎN modal — `handlePay` întoarce `PayResult`, `advance` cu `throwOnError`; pre-flight INFORMATIV (nu blocant) pe suma de pe notă = înmânată − bacșiș, oglindind exact fallback-ul butonului (`parseFloat(amount) || grandTotal`); `.catch` ÎNAINTE de `.finally`. (5) Copy-ul e-Factura nu mai promite trimiterea în SPV: Menuvia afișează ce întoarce Oblio la emitere (`has_einvoice` = prezența XML-ului, NU starea din SPV); badge-ul negativ e AVERTISMENT (gold), nu eroare. RES-26 (fallback pe planul din cache la cold start) a fost REFUTAT ca feature: tristate-ul e fail-closed deliberat, cu recuperare vizibilă (Reîncearcă).
- **/health are cinci sonde, iar corpul PUBLIC e exact `{status, checks, ts}` (mig 271 + health.js, audit v3 RES-08/RES-32/RES-38A)**, teste SV1–SV6 `tests/sql/schema_version_assertions.sql`, QB1–QB8 `tests/sql/queue_backlog_assertions.sql`, HL8–HL19 `tests/functions/health.test.js`, SM1–SM2 `tests/functions/schema-manifest.test.js`. (1) **Sonda de schemă** `get_schema_version(text[])` primește manifestul comis `netlify/functions/schema-manifest.json` (numele fișierelor din `supabase/migrations/` FĂRĂ prefixul de 14 cifre — regenerat cu `node scripts/gen-schema-manifest.mjs`; SM1 pică pe orice PR care adaugă o migrație fără regenerare) și întoarce ce LIPSEȘTE din `supabase_migrations.schema_migrations`. **Cheia e `name`, NU `version`**: pe prod `version` e timestamp-ul aplicării prin MCP (269 = `20260905075533`), deci o comparație pe version ar da „behind" permanent (SV2). **DEFINER obligatoriu**: `service_role` n-are USAGE pe schema `supabase_migrations` — INVOKER ar întoarce `available=false` pentru totdeauna (SV5, doar catalogul vede clasa). Fără ledger (CI-ul efemer) → `available=false` FĂRĂ excepție (SV1). `schema:'behind'` NU schimbă codul HTTP (deploy-înaintea-migrației e tranzit legitim) — `health-watch.yml` pică roșu pe el (singurul canal cu email azi); numele lipsă doar cu token. (2) **Sonda de backlog** `get_queue_backlog()` — de BACKLOG (muncă scadentă neridicată), nu heartbeat per job (orb la „funcția rulează, întoarce 200 și nu face nimic"). **Fiecare predicat OGLINDEȘTE claim-ul** (email/sms 242/228: `queued`, scadent, `failed_attempts<3`; invoices 269: + `oblio_configs.is_active` + fereastra `next_attempt_at`; remindere 234: AMBELE canale + `reminder_hours_before`; slack 175 doar raportat) — QB3–QB6 fac claim-ul REAL și cer 0 după (o sondă mai largă decât claim-ul = alarmă permanentă falsă; mai îngustă = sondă moartă). Două grupe deliberate: `cron` (platformă → `stale` = 503: email 30 min, sms/invoices 60, remindere 120) și `bridge` (PC-ul unui restaurant → DOAR `warn`, 200 — alarma per-tenant e bannerul 265). `data` null/fără formă → `unknown`, NICIODATĂ `ok`. DEFINER deliberat (grant-urile service_role pe cele 7 tabele sunt eterogene), grant EXCLUSIV service_role (volumul cozilor = volum de business). Capcană de test: baseline-ul replay-ului poate conține rânduri; după claim sonda TREBUIE să dea 0, nu „baseline"; într-un DO block nu numi variabila ca o coloană din temp table (`v` vs `qb_base.v` → „ambiguous"). (3) **Public = severitate, cu token = cifre, pe TOT corpul**: `config` (ce integrări sunt moarte), `cron_last_run`, `storage_detail`, `schema_detail`, `queue_detail` apar DOAR cu `x-health-diag`; forma publică e ÎNGHEȚATĂ de HL8 (`Object.keys(body) == ['checks','status','ts']`), nu verificată pe câmpuri știute — un câmp nou scurs public pică; ramura 503 „env lipsă" pierde și ea `config` (HL9). Testele HL4–HL6 verifică ABSENȚA cheii (`'storage_detail' in body === false`), nu `null`. Mutații verificate: fără join `oblio_configs` → QB5; fără `failed_attempts<3` → QB2; scurgerea unui câmp public → HL8. **Rundă de recenzie adversarială pe #246 (CodeRabbit rate-limited, recenzia a fost proprie), patru clase închise cu mutații dovedite pe replay:** (a) **probe NARROWER decât claim-ul** — email/sms numără și rândurile blocate în `sending` >10 min pe care claim-urile 242/228 le RECLAMĂ (`failed_attempts + 1 < 3`; vârsta lor = de la `claimed_at`); fără ele, un worker ucis mid-batch + cron oprit = coadă „0" cu emailuri nelivrate (QB2/QB4 seed-uiesc un `sending` de 20 min → numărat, unul de 2 min → nu, unul la plafon după bump → nu); (b) **probe WIDER decât claim-ul** — bonuri/tichete pending DOAR la restaurante cu `bridge_devices` (247) și, pentru tichete, device cu `prints_kitchen_receipts` + feature `kitchen_tickets` (227); un rând orfan (device șters — `bridge_device_id` e SET NULL, rândul rămâne; plan retrogradat) nu mai e `warn` PERMANENT (QB7: device-ul pe free e refuzat de gate-ul 149/227, deci scenariul se construiește prin DOWNGRADE după inserare); (c) **vârsta reminderelor se măsoară de la CLAIMABIL**, `greatest(starts_at - fereastră, created_at)` — o rezervare same-day făcută ÎN fereastra de 24h raporta 68.400 s (19h) în secunda inserării → 503 fals până la următorul tick `*/30` (QB6: rând creat acum pentru un slot peste 5h → vârstă 0; NU `updated_at` ca podea — reclaim-ul 215 și resetul worker-ului îl bumpează); (d) **health.js: contract COMPLET sau `unknown`** — `{cron:{}, bridge:{}}`, array-uri, o cheie de vârstă redenumită sau o coadă lipsă dădeau `ok` (age() cădea pe 0), iar `{available:true}` fără `missing` dădea `schema:ok` — exact alarma moartă; acum fiecare coadă din `QUEUE_STALE_S`/`BRIDGE_QUEUES` trebuie să fie obiect cu `oldest_age_s` numeric, `missing` trebuie să fie array (HL13/HL18 cu control pozitiv; HL16 parametrizat pe TOATE pragurile, HL17 și pe `tickets` — înainte ștergerea `sms`/`invoices` din praguri trecea). Cele patru sonde tolerante rulează ÎN PARALEL (`Promise.all`): în serie, ping + 4 × 4 s = 20 s depășea limita sincronă de 10 s a Netlify → 502 FĂRĂ corp pe o bază lentă-dar-vie; acum plafonul e 8 s, semantica per sondă neschimbată.
- **Cota TVA a liniei de comandă e SNAPSHOT la vânzare (mig 272, audit v3 RES-20)**, teste VS1–VS8 `tests/sql/vat_rate_snapshot_assertions.sql` + OV1–OV5 `tests/functions/oblio-generator.test.js`. `products.vat_group` NU e o cotă, e o REFERINȚĂ la `vat_rates` (029), a cărei `rate_percent` e editabilă (VatRatesEditor) și s-a schimbat o dată prin lege (102: 9→11, 19→21, doar default-urile); `order_items` avea snapshot pe nume/preț (003) dar NU pe TVA, deci toate cele trei cititoare re-rezolvau LIVE `order_items → products.vat_group → vat_rates`: `vat_report_daily` (istoricul se rescria la orice schimbare de cotă/reclasificare — raportul nu mai corespundea cu Z-urile), `build_fiscalnet_payload` (la REGENERARE — retry 262/270, INSERT-direct-paid 259 — linia sărea în altă grupă a casei) și `oblio-generator.js` (la EMITERE, care poate fi la ZILE de încasare, 218/239). Cazul cel mai des: produs ȘTERS după vânzare → FK `set null` → `coalesce(p.vat_group, 1)` → toată istoria lui trecea TĂCUT la grupa 1 (VS5). **Ce encodează FiscalNet**: `S^…^buc^GRUPA^1` poartă INDEXUL grupei programate ÎN CASĂ (1–5, „setată de instalator"), NU o cotă — deci snapshot-ul util pe bon e GRUPA INTERNĂ, iar maparea `vat_rates.fiscalnet_group` rămâne LIVE (config de DEVICE: o corecție de instalator se aplică și la retry, VS4b); cota (RATE) e ce cer raportul și Oblio (`vatPercentage`+`vatName`). De aceea sunt DOUĂ coloane: `vat_group_snapshot` (1–4) + `vat_rate_snapshot` (numeric(5,2)), completate de trigger BEFORE INSERT `trg_snapshot_order_item_vat` (tgtype **7** EXACT — un `or update` = 23 ar re-snapshot-ui la UPDATE-uri; DEFINER `public, pg_temp`; completează DOAR ce lipsește; cota pe restaurantul COMENZII, ca cititorii) — un trigger, nu recrearea `create_order` (191)/`update_order_items` (192), care rămân NEATINSE (inserează cu listă explicită de coloane) și acoperă orice scriitor viitor (precedent 130). Cititorii: `coalesce(oi.vat_group_snapshot, p.vat_group, 1)` / `coalesce(oi.vat_rate_snapshot, vr.rate_percent, 0)` — ORDINEA contează (M3: produs întâi → VS3 pică); fallback-ul pe cota curentă e DOAR pentru rânduri fără snapshot (produs șters înainte de backfill), documentat de VS6: după o schimbare de cotă o grupă se SPARGE în rândul cu snapshot (cota veche) și rândul fallback (cota nouă). **Ziua de raportare e ziua ROMÂNEASCĂ a încasării (mig 272, găsit de recenzia adversarială pe #251, VS10)**: lanțul moștenise `date_trunc('day', o.paid_at)` FĂRĂ conversie, deci se rezolva în TimeZone-ul SESIUNII — iar pe Supabase acela e UTC (verificat pe producție: `current_setting('TimeZone')` = `UTC`). O încasare la 00:30 ora României pica pe ziua PRECEDENTĂ, iar la graniță de lună în PERIOADA FISCALĂ precedentă, în timp ce VatReportTab cere intervalul cu `toRomaniaYMD` și 267/268 raportau aceeași încasare pe ziua românească — deci raportul de TVA și cel de venit nu puteau reconcilia pe o lună. Aceeași clasă reparată de 269 pentru `deliveryDate` la Oblio. VS10 cere ziua românească ȘI independența de TimeZone (aceeași interogare sub UTC și sub Australia/Sydney) ȘI coincidența cu `v_daily_payments_by_method` — mutația „zi fără conversie" pică. Lanțuri: `vat_report_daily` 028→029→031→125→150→238→253→**272** (formă de coloane ÎNGHEȚATĂ — VatReportTab face `select('*')`; `create or replace view` refuză schimbarea de tipuri, verificat identic pe replay), `build_fiscalnet_payload` 030→050→051→052→053→**272** (copie verbatim 053 — B1–B4/A1–A5/BUG#6 — + snapshot; suprafață 262: doar service_role, `public, pg_temp` scris direct). Editarea pre-plată (`update_order_items` = DELETE+INSERT, doar comenzi ne-terminale fără parțiale) RE-snapshot-uiește la cota curentă (VS7) — corect: momentul fiscal e bonul. **Backfill = grupa/cota CURENTĂ a produsului** (cea mai bună informație: `vat_rates` NU are date de intrare în vigoare și NU e auditată de 044 — doar `products`; producția a pornit după L.141/2025, deci schimbarea legală nu cade în istoric) — rulat cu `order_items_subtotal_sync_upd` DEZACTIVAT per-trigger (248: orice UPDATE pe order_items → `_refresh_order_totals` → UPDATE pe comenzi PLĂTITE → audit_orders + „repararea" TĂCUTĂ a unui total deja bonat) și cu audit-ul per rând sărit prin GUC-ul `menuvia.skip_item_audit` (081); verificat pe date sintetice: un total driftat rămâne neatins, zero rânduri de audit, liniile fără produs rămân NULL. Înainte de `db push` pe prod rulează `select r.slug, vr.vat_group, vr.rate_percent, vr.updated_at, min(o.paid_at) from vat_rates vr join restaurants r on r.id=vr.restaurant_id join orders o on o.restaurant_id=r.id and o.status='paid' group by 1,2,3,4 having vr.updated_at > min(o.paid_at)` — rândurile listate au vânzări DINAINTEA ultimei editări a cotei și vor primi cota de azi (decizie de fondator; `audit_log` pe `products` permite reconstrucția reclasificărilor, nu și a cotelor). **Rulat pe producție la 11 sept 2026: ZERO rânduri** — niciun restaurant nu a editat o cotă după prima încasare, deci „cota curentă" = „cota de la vânzare" pentru tot istoricul de azi și backfill-ul nu cere nicio decizie. **Orfanii au script propriu, rulat MANUAL**: `scripts/recover_orphan_vat_snapshots.sql` (NU e în lanț) recuperează grupa liniilor al căror produs a fost ȘTERS înaintea migrației — acelea n-au nici `product_id`, nici snapshot, deci cad pe grupa 1. Sursa e `audit_log` pe `products` (mig 044), potrivit pe (restaurant, `product_name_snapshot`); `order_items` NU are rânduri de audit pentru ele, deci numele e singura punte. **Se citește TOT istoricul numelui, nu doar rândul DELETE** (recenzie CodeRabbit pe #251): un produs RECLASIFICAT înainte de ștergere face ca ștergerea să raporteze o grupă pe care vânzarea nu a avut-o — adică exact defectul reparat de 272, strecurat înapoi prin ușa din dos. Istoricul ia grupa din AMBELE instantanee (`old_data` + `new_data`, cu guard `~ '^[0-9]+$'` — `old_data` poate conține orice) și include și produsele care încă EXISTĂ cu acel nume. Măsurat pe producție: 16 din 53 de linii sunt orfane (447,00 lei, TOATE pe comenzi `paid`, un singur restaurant `enterprise` → intră în raportul TVA), iar una dintre potriviri („Vin pahar", 36,00 lei) era grupa **2**, raportată azi ca grupa 1. Un nume care a purtat VREODATĂ grupe DIFERITE se SARE, nu se ghicește (a ghici într-un jurnal fiscal dă unei presupuneri aparența de certitudine; a sări lasă linia exact cum e azi). Scriptul are DOI pași — previzualizare (IMPLICITĂ: raportează din ACEEAȘI logică, nu scrie și NU ia lacăt) și aplicare, care cere explicit `set menuvia.recover_apply='on'` — fiindcă `alter table ... disable trigger` ia `SHARE ROW EXCLUSIVE` pe `order_items`, care blochează INSERT/UPDATE/DELETE, adică CREAREA DE COMENZI pe toată platforma: ținut peste o fereastră de decizie umană (varianta „interactiv cu begin … commit" pe care o propusesem în runbook) ar fi oprit comenzile la toate restaurantele. Aplicarea verifică la final că a scris exact câte linii anunțase previzualizarea. Steagul e FAIL-CLOSED (implicit previzualizare, valoare nerecunoscută → eroare): invers, un nume de GUC scris greșit — Postgres acceptă tăcut orice `set prefix.nume` — ar fi ales ramura care SCRIE într-un jurnal fiscal, iar pasul de previzualizare rulează fără `-1`, deci s-ar fi și comis. Drop-urile tabelelor temporare sunt calificate `pg_temp.` — necalificat, `drop table if exists _orphan_candidates` cade prin search_path și șterge o tabelă PERMANENTĂ omonimă (exact genul rămas dintr-o reparație manuală anterioară). VS9a aserteză toate trei: fără steag nu se scrie NIMIC, nu apare niciun `ShareRowExclusiveLock` pe `order_items` în `pg_locks`, iar o santinelă `public._orphan_candidates` supraviețuiește. Scriptul e idempotent (atinge doar `vat_group_snapshot is null`), dezactivează `order_items_subtotal_sync_upd` și sare auditul per rând ca backfill-ul 272, iar dezactivarea e într-un `do … exception when others then … raise` care REACTIVEAZĂ pe calea de eroare — dovedit empiric: structura naivă (`alter table disable` ca instrucțiune separată) rulată cu `ON_ERROR_STOP=1` dar FĂRĂ `-1` lasă trigger-ul STINS în producție (subtotalurile ar înceta tăcut să se sincronizeze), forma cu handler îl lasă pornit, și e acoperit de VS9 prin `\i` din suită — mutațiile „fără filtrul de ambiguitate" și „fără potrivire" pică amândouă. **Oblio**: `oblio-generator.js` CERE în select `vat_rate_snapshot`/`vat_group_snapshot`/`product_name_snapshot` (OV5 e clichet pe SELECT — fake-ul întoarce orice, deci fără el un select care nu mai cere coloanele trecea cu fallback tăcut pe cota curentă) → funcția se deployează DUPĂ migrație (pe DB veche: 42703 → `mark_failed`, retryabil; `invoices` e goală în prod, fereastră inofensivă). Mutații dovedite pe replay: view vechi → VS2; precedență greșită pe grupă → VS3; payload vechi → VS4; fără trigger / trigger cu grupă fixă → VS1; JS vechi → OV1–OV5. **`security_invoker` pe `vat_report_daily` are acum clichet PERMANENT (VS8 pe catalog + VS11 sub RLS)**: era verificat DOAR în corpul migrației, adică o singură dată, la poziția 272 din lanț — clasa DP6/F1–F9. Mai rău, antetul suitei delega proprietatea către „rls_scoping_batch2 / report_revenue_gate": prima nu pomenește view-ul, a doua NU EXISTĂ — documentație care minte peste un gate mort. View-ul e deținut de `postgres` și are `grant select ... to authenticated`, deci fără `security_invoker` rulează cu drepturile proprietarului: verificat prin mutație (DROP+CREATE fără opțiune — `create or replace` nu o poate schimba) că un cont autentificat FĂRĂ nicio apartenență citește 3 rânduri din raportul TVA al altui restaurant. VS11 rulează sub rolul REAL `authenticated` (ca PM8/TP24 — restul suitei e `postgres`, care ocolește RLS și e ORB la clasa asta) și are control POZITIV pe owner, altfel „0 rânduri" ar trece și dintr-o eroare de drepturi; verificat că prinde ȘI cu clichetul VS8 dezactivat, ca să nu fie mascat de el. Rezidual cosmetic, NEadresat: VatReportTab agregă cardurile pe `vat_group` — după o schimbare de cotă o grupă are 2 rânduri (cote diferite), sumele rămân corecte, doar eticheta cardului e a primului rând (cel mai nou); cheia pe (grupă, cotă) e o schimbare de client de 3 linii, la următorul deploy de client. Rezidual fiscal, NEadresat: la un retry DUPĂ o schimbare LEGALĂ de cotă, casa aplică cota ei programată CURENTĂ pentru grupă (formatul nu transportă cota) — un bon la cota veche cere intervenție manuală pe casă, nu cod.
- **Rezervarea publică e IDEMPOTENTĂ pe (restaurant, cheie) (mig 273, audit v3 RES-29)**, teste RI1–RI8 `tests/sql/reservation_idempotency_assertions.sql` + R1–R9 `src/lib/__tests__/reservations-idempotency.test.ts`. Era singura scriere publică fără cheie (QR și PICKUP o aveau deja): un dublu-tap, o revenire cu Back sau o retrimitere după un răspuns pierdut pe mobil crea A DOUA rezervare — două mese blocate, două emailuri către local și, pe `auto_confirm`, două SMS-uri către client. Trei straturi, de la cel mai slab la cel mai tare: (1) `reservations.idempotency_key uuid` + **index UNIC PARȚIAL** pe `(restaurant_id, idempotency_key) where not null` — garanția reală, independentă de cod; (2) LOOKUP în RPC care întoarce rândul existent, **plasat ÎNAINTEA validărilor de setări** — altfel o retrimitere la 30 s distanță ar pica pe `min_advance_hours`, care devine adevărat cu trecerea timpului (RI6; mutația „fără lookup" o face să pice); (3) BACKSTOP `exception when unique_violation` în jurul INSERT-ului pentru cursa a două cereri simultane, care **RE-ARUNCĂ** violările străine (`raise;`) — un handler care înghite orice unique_violation ar transforma o coliziune de `confirmation_code` într-un fals „a mers" (RI8). Lookup-ul și backstop-ul sunt AMBELE scopate pe `restaurant_id`: fără asta, aceeași cheie la alt local ar întoarce rezervarea primului (RI7). **Emailul, SMS-ul și plafonul anti-abuz se sar automat**: `trg_email_reservation_created` (AFTER INSERT), `trg_sms_reservation_confirmed` (AFTER INSERT OR UPDATE) și `reservations_public_rate_limit` (BEFORE INSERT) sunt toate pe INSERT, iar calea idempotentă nu inserează — RI4 dovedește că 5 retrimiteri de la același telefon trec fără să atingă plafonul de 3/5 min (pe codul vechi a treia ar arunca). **Semnătură NOUĂ → DROP + CREATE**: un `create or replace` cu un parametru în plus lasă ambele semnături și PostgREST dă PGRST203 la ORICE apel (ca `register_affiliate` 243); parametrul e ULTIMUL și are default, deci clientul se poate deploya înainte SAU după migrație. Lanț `create_reservation_public` 151→199→201→241→**273** — orice recreare pornește de AICI și păstrează TOT (p_table_id 199, plafon de durată 151, gate de modul 200, wrap-around 201, ziua de SERVICIU 241). Client: `lib/idempotency.ts` (`createIdempotencyKeyStore`, fabrica pe care o folosesc acum ȘI cheia pickup ȘI rezervarea — QR-ul a rămas pe implementarea veche, fără fallback din memorie, fiindcă migrarea lui schimbă comportamentul în private mode), `lib/reservations.ts` (cheie pe slug, rotită DOAR pe succes; `createReservationPublic` aruncă `Error` real cu `hint`/`code` ca `createOrder`, și are fallback O SINGURĂ DATĂ pe PGRST202 la contractul vechi, ca un client deployat înaintea migrației să nu rupă rezervările). **Capcană de aserție prinsă la prima rulare**: o verificare de ORDINE prin `position(...)` pe numele unei coloane a picat fiindcă un COMENTARIU o pomenea mai devreme decât codul — ancorează pe expresia din cod (`(v_settings.min_advance_hours ||`), nu pe identificator, și verifică separat că ancora există.
- **Două capcane la scoaterea unui apel `supabase` dintr-o componentă** (costate un CI roșu pe #252): (1) un `grep -c 'supabase\.'` NU prinde apelurile multilinie (`supabase\n  .from(...)`) — verifică pe `grep -c 'supabase'`, altfel ștergi un import încă folosit și pici cu TS2304 abia în CI, fiindcă fără `node_modules` nu poți rula `tsc` local; (2) `Restaurant.slug` e **opțional** (`slug?: string` în qr.ts) — mergea până acum doar fiindcă payload-ul `supabase.rpc` e netipat; prima funcție TIPATĂ care îl primește cere îngustare explicită (`const slug = restaurant.slug; if (!slug) return`). Cheile de idempotență se scopează pe `restaurant.id`, nu pe slug: id-ul e ne-opțional ȘI stabil, pe când slug-ul se poate schimba prin `change_restaurant_slug` — o cheie al cărei scope se schimbă e o cheie NOUĂ, adică exact dublura pe care o previi.
- **Capcană typecheck fără node_modules**: tsc-ul standalone (global, `--ignoreConfig`) NU vede erorile care depind de tipurile React — `useState` devine `any`, deci **narrowing-ul pe state dispare**. Exemplu real (6 build-uri roșii, iulie 2026): cod după early-return-ul `if (confirmation) return ...` vedea `confirmation` narrowed la `null` pe Netlify → acces pe `never` → TS2339, invizibil local. Când un preview pică fără eroare locală: caută întâi accese pe state DUPĂ un early-return care îl exclude (cod mort), abia apoi bisectează pe preview-uri (sursă-identică-cu-verde = testul cod-vs-mediu). A doua variantă reală (iulie 2026, i18n E4a): `useState(t.cheie)` cu inițial dintr-un obiect `as const` → pe Netlify state-ul se inferează la TIPUL LITERAL („Feluri principale" | „Main Courses") și `setX(e.target.value)` pică cu TS2345 — orice `useState` cu inițial din tabele de traduceri/constante `as const` cere adnotare explicită (`useState<string>(...)`). A treia variantă reală (iulie 2026, E5b): builderul supabase-js e doar `PromiseLike` — un lanț `supabase.rpc(...).then(...).then(...)` anotat `Promise<...>` pică TS2322 DOAR pe Netlify (local, fără tipuri, totul e `any`); folosește `async/await` într-o funcție `async` (întoarce Promise real), nu lanțuri .then pe builder.
- **Capcană JSX: un `"` DREPT într-un atribut JSX `attr="..."` sparge build-ul** (`Unterminated string literal`, TS1002/1003) — string-urile RO conțin des `"`/„…". Când un atribut (aria-label/title/placeholder/description) conține `"`, folosește `attr={'...'}` (expresie cu ghilimele simple) sau backtick, NU ghilimele curbe într-un `"..."`. `tsc` prinde, dar preview-ul Netlify pică rapid — semnal de eroare TS reală.
- **`get_order_audit_history` (orders.ts) aruncă un `Error` real** (nu PostgrestError brut) — altfel `OrderAuditSheet`, care testează `e instanceof Error`, cade pe „Eroare la încărcare" generic și ascunde cauza (funcție nedeployată / permisiune). Păstrează `err.code` pentru diagnoză. Același pattern ca `createOrder`.
- **Root-ul e ESM (`"type":"module"`)** — orice cod CommonJS din repo TREBUIE să stea sub un `package.json` cu `"type":"commonjs"`: există în `bridge/`, `deploy/` și `netlify/functions/`. Fără marker, `require()` pe acele fișiere `.js` pică cu ReferenceError (prins la testarea shim-ului VPS).
- **Validare SQL fără CI**: `bash scripts/verify-migrations-local.sh` = replay-ul complet al lanțului de migrații + asserțiile din sql-verify.yml pe un Postgres 16 efemer local (bootstrap-ul Supabase-compat se extrage DIN workflow — sursă unică). Sare intenționat cele 2 teste de eră (phase_1a/final_state 096) pe care și CI-ul le sare prin `if: hashFiles(...)`. Din sept 2026 rulează ȘI asserțiile INLINE din workflow (heredoc-uri), nu doar fișierele `tests/*.sql`: golul a costat un PR roșu — mig 264 trecea local 82/82 si pica in CI pe blocul „Gate C", care încă folosea contractul de bani dinaintea mig 214/262 (`p_paid_amount` e suma ÎNMÂNATĂ, notă + bacșiș).
- **Split pe itemi (mig 229) peste plata online**: `begin_split_payment`/`get_table_bill` = SECURITY DEFINER **doar service_role** (prin table-payment.js, ca restul suprafeței); `begin_table_payment` RĂMÂNE neredefinit deliberat (supersede-ul F3 din 211 acoperă și split-urile). `order_payments.method` include acum `'card_online'` și `'meal_voucher'` (mig 230/231 — tichete de masă, staff-recorded, cod P 4), dar `add_partial_payment` (lanț 017→111→231) NU acceptă `card_online` (staff-ul nu înregistrează plăți online manual — asserție în ambele migrații); `fiscalnet_payment_code` are lanț 030→…→203→231. Claims ținute și pe `'failed'` (retryable, mig 207) — NU adăuga supersede agresiv pe split-urile altora (fereastră de dublă încasare); eliberare = cancel_table_payment + TTL 15 min doar pe `created` fără intent. `table_payment_items` FĂRĂ FK pe order_items (editările de staff nu evaporă claims cu bani). `orders.payment_method` e ENUM — orice CASE nou pe el cere cast explicit. Bon per plătitor = v2. Teste TP13–TP20 în același fișier cu TP1–TP12.
- **Întărire securitate din audit (mig 258)**: trei fix-uri chirurgicale, fiecare cu asserție fail-closed în migrație + suită `tests/sql/security_hardening_258_assertions.sql` (SH1–SH5). (1) `add_partial_payment` (lanț extins 017→111→231→**258**) a primit PLAFON DE SUPRA-ÎNCASARE (paritate cu overpayment-ul din advance_order mig 214/243): `sum(order_payments)+p_amount > total+0.01` → `raise` cu hint `overpayment`, ÎNAINTE de insert; fără el un typo de staff (5000 vs 50.00) corupea tăcut paid_amount/ReportsTab/TVA. Orice recreare păstrează plafonul + TOATE gate-urile vechi (whitelist metodă incl. meal_voucher, card_online respins, gate fiscal, gate rol, cast enum). (2) `get_loyalty_state` (lanț 226→258) a devenit VOLATILE (înainte STABLE) și are rate-limit `check_rate_limit('get_loyalty_state', qr_token_id, 40, 5)` anti-enumerare de telefoane; peste plafon întoarce `{enabled:true, rate_limited:true}` FĂRĂ short_code/points (nu distinge înrolat de neînrolat). (3) `security_ownership_remediations` a primit RLS deny-all (înainte doar revoke) — defense-in-depth contra unui GRANT viitor accidental. **Invariant #1 din audit, verificat empiric + înghețat de SH3**: un `authenticated` NU poate ridica `profiles.is_platform_admin` (mig 096a re-acordă UPDATE doar pe 4 coloane, flag-ul nu e în set → SQLSTATE 42501). NU re-acorda `update` la nivel de tabel pe `profiles` — ar redeschide escaladarea (asserția din mig 258 pe `has_column_privilege` prinde regresia). **Plafoane pe suprafața anon de analytics (mig 261, lanț 013→261 + 097c→261)**: `record_qr_scan` 300/15min per restaurant, `record_page_view` 600/15min + product_id străin ANULAT la NULL (anti-poluare cross-tenant), `preview_referral` 600/15min GLOBAL (`'all'` — fără IP în RPC, per-cod n-ar opri rotația). Toate degradează TĂCUT peste plafon (analytics, nu bani) și păstrează grant-urile anon. Teste AR1–AR4: `tests/sql/anon_rate_limit_assertions.sql`.
- **Audit v3 — hardening (mig 262, sept 2026)**, cu suita AV1–AV10 `tests/sql/audit_v3_hardening_assertions.sql`. (1) **`profiles` NU mai acceptă INSERT/DELETE din roluri client**: mig 047 dăduse CRUD complet, 096a revocase DOAR UPDATE → orice cont putea să-și ȘTEARGĂ rândul și să-l reinsereze cu `is_platform_admin=true` + `plan='enterprise'` (preluare de platformă + Plan 3 gratuit; SH3 testa doar UPDATE). Politica FOR ALL `profiles_self` e spartă în `profiles_self_select` + `profiles_self_update`; trigger `trg_profiles_block_client_write` (rulează cu privilegiile apelantului, respinge `current_user in ('anon','authenticated')`) e backstop-ul contra unui GRANT viitor. INSERT-ul legitim = `handle_new_user` (DEFINER), DELETE-ul = cascada `auth.users`. NU re-acorda INSERT/DELETE la nivel de tabelă pe `profiles`. (2) **Helperii interni NU sunt executabili de anon**: `_refresh_order_totals`, `build_fiscalnet_payload`, `owner_plan`, `get_restaurant_features`, `log_ai_import`, `reserve_ai_import_slot`, `check_ai_import_quota` — revoke public+anon+authenticated, grant service_role; `authenticated` păstrează DOAR `get_restaurant_features` (singurul cu apelant client, `useFeatures`) — toți ceilalți apelanți sunt trigger-e/RPC-uri DEFINER (verificat pe replay). Orice helper nou: `revoke ... from public` din start. `get_restaurant_by_qr_token` (anon, proiecta `wifi_password`) e DROP-uit; asserția din 262 verifică că NICIO funcție anon nu conține `r.wifi_password`/`r.qr_token`. (3) **Politicile pe `products` au roluri explicite** (`public read active` → `to anon, authenticated` + `is_restaurant_active`; restul `to authenticated`) — o politică pe rolul PUBLIC care apelează `is_member`/`is_admin` dă `permission denied` pentru anon și omoară fallback-ul `fetchMenuLayered`. `invite_tokens`: citirea e `is_admin` (era `is_member` → waiterii vedeau emailurile + token-urile). (4) **`bridge_retry_receipt` (lanț 030→038→262) REGENEREAZĂ payload-ul** din comanda curentă (rândurile 'error' cu payload '' din mig 259 intrau în buclă EMPTY_PAYLOAD); build eșuat → hint `payload_build_failed`. **`bridge_mark_stale_as_error` (030→035→262)** scrie markerul `POSIBIL DUPLICAT` (BridgeTab cere verificarea benzii) și e chemată ORAR din automation-cron (Job 1f, service_role) — înainte nu o chema nimeni și bonurile agățate în 'sent' rămâneau așa pentru totdeauna. (5) **`enqueue_invoice_for_order` (041→262)** respinge cu `ambiguous_failed_exists` o comandă cu factură `failed` al cărei `last_error` începe cu `POSIBIL DUPLICAT`/`STUCK_GENERATING` (Oblio POATE fi emis-o deja → a doua cerere = duplicat fiscal); IssueInvoiceModal oglindește filtrul. Un `failed` clar (4xx) rămâne re-emisibil. (6) **`resolvePlan` (stripe-webhook.js) verifică `sub.status`** (`active/trialing/past_due`, altfel `free`) și preferă `PLAN_BY_PRICE` din `items` — un `checkout.session.completed` replay-uit (webhook mort → Resend din Dashboard) pe un abonament anulat NU mai acordă Plan 3 permanent. (7) **send-invite.js citește numele restaurantului/invitatorului din DB**, nu din body (relay de email brandat). (8) Igienă: TOATE funcțiile DEFINER cu `search_path = public` au primit `public, pg_temp` prin ALTER FUNCTION (asserție: zero DEFINER fără pg_temp) — funcțiile noi se scriu direct cu `public, pg_temp`. Refuzat la verificare: `bridge_confirm_receipt` ARE deja gardă pe device/status (mig 045).
- **Audit v3 — consiliul (mig 264, sept 2026)**, teste AC1–AC6 `tests/sql/audit_v3_council_assertions.sql`. Cele două echipe roșii au atacat REMEDIERILE din 262/263 și au găsit unde nu închid clasa. (1) **Plafonul de încasare e acum și INFERIOR** (`underpayment`, ambele ramuri din `mark_paid`): 262 pusese doar plafonul superior, dar o sumă SUB rest lăsa `sum(order_payments) < total`, guard-ul B3 din `build_fiscalnet_payload` (mig 053) refuza payload-ul, iar comanda rămânea `paid` cu bon IMPOSIBIL de emis — aceeași consecință ca MF-01, pe direcția opusă. Încasarea în tranșe are calea ei: `add_partial_payment`. (2) **Gate-ul fiscal de închidere a coborât din RPC în DATE**: `trg_orders_closed_fiscal_gate` (oglinda exactă a mig 124 pentru →`closed`) — politica `orders: admin all` permitea un `PATCH /rest/v1/orders {status:'closed'}` direct prin PostgREST, care ocolea gate-ul din 263. Verificat: SINGURII scriitori de `status='closed'` sunt `advance_order` și `close_session_orders`, deci trigger-ul nu rupe niciun flux legitim (`expire_inactive_sessions` atinge doar `table_sessions`). (3) **`product_extras`/`product_pairings`**: mig 262 reparase SEC-04 doar pe `products`; tabelele-soră au rămas cu politici FOR ALL pe rolul PUBLIC care apelează `is_admin` → verificat pe PRODUCȚIE ca anon: `42501 permission denied for function is_admin`, adică fallback-ul `fetchMenuLayered` era MORT pentru clientul anonim. **Tiparul contează mai mult decât cazul**: asserția nu mai e literală pe un tabel, ci de CLASĂ — pentru orice tabel cu SELECT pentru anon, nicio politică aplicabilă la SELECT nu are voie să stea pe rolul PUBLIC și să apeleze funelul (verificarea privilegiului se face în corpul buclei, NU în `where`: planificatorul reordonează predicatele și a picat o dată pe un `pg_toast_*`; sursa e catalogul `pg_policy` cu `polrelid` — un OID, fără round-trip prin nume — și `0 = any(polroles)`, ECHIVALENT cu `pg_policies.roles = '{public}'` fiindcă Postgres normalizează orice listă cu PUBLIC la exact `{0}` „ignoring specified roles other than PUBLIC", verificat empiric pe PG 16 și pe producție). (4) `package.json` are `"prepare": "husky || true"` — cu `husky` simplu, `npm ci --omit=dev` din `deploy/setup-vps.sh` murea la pasul 4/8, deci singura cale de salvare a cron-urilor era ruptă de o linie.
- **⚠️ DECIZIE DE FONDATOR deschisă — bacșișul în sertarul de casă.** Mig 262 a scos bacșișul din `orders.paid_amount` (corect pentru bon și rapoarte), dar `cash_collected_for_shift` (mig 032) însumează exact `paid_amount` pentru comenzile cash fără split. Efect: „așteptat în sertar" NU mai include bacșișul cash, deci fiecare tură cu bacșiș arată SURPLUS la numărătoare. Care variantă e corectă depinde de practica localului (bacșișul intră în sertar și se împarte la final, sau rămâne la ospătar). Până la decizie NU modifica `cash_collected_for_shift` unilateral: opțiunile sunt (a) adaugă `tips_amount` cash ca linie SEPARATĂ în `get_shift_summary`, sertarul rămânând net; (b) include bacșișul în așteptat + o mișcare de casă „bacșiș predat" la închidere.
- **Audit v3 — lotul 2 (mig 263 + client, sept 2026)**, teste AB1–AB4 `tests/sql/audit_v3_batch2_assertions.sql`. (0) **Regula de aur are acum gate în AMBELE căi de închidere**: `advance_order` ramura `close_order` ȘI `close_session_orders` (lanț 085→087→263) resping închiderea nefiscală pe planurile cu `fiscal_receipt` (hint `fiscal_plan_requires_payment`); gate-ul din `close_session_orders` fires DOAR dacă sesiunea are comenzi neterminale. Nu blochează niciun flux legitim fiindcă pe Plan 3 masa se eliberează SINGURĂ: `trg_maybe_close_session` (mig 084) închide sesiunea când ultima comandă intră în `paid`/`cancelled` — lista lui NU include `closed`, deci pe growth `close_session_orders` rămâne calea normală. (1) **`paymentsEnabled` din WaiterPage e TRISTATE** (`boolean | null`): `null` = planul nu e cunoscut (features neîncărcate/RPC picat) → OrderCard nu arată NICI „Plată", NICI „Închide comanda" + banner cu Reîncearcă. Înainte necunoscutul cădea pe `false` → „Închide comanda" (nefiscal) pe Plan 3, acceptat de server → comandă `closed` fără bon (DS-1). Serverul are gate propriu (`fiscal_plan_requires_payment` în `close_order`). (2) **Sesiunea QR se REHIDRATEAZĂ** din `sessionStorage` (`lib/qrSession.ts`, TTL 6h): `previousOrders` (+ confirmarea curentă), `sessionId`, `paidOrderIds` — după evicție iOS/back/refresh bannerul de urmărire și „Plătește online"/split rămân (FC-02). Cheia de idempotență PICKUP e și ea persistată (`getPickupIdempotencyKey(slug)`, rotită pe succes — FC-01: înainte stătea într-un `useRef` care murea cu sheet-ul → comandă dublă la retrimitere). (3) `useSWUpdate` NU mai face reload la PRIMA instalare a SW (doar la o schimbare de controller ULTERIOARĂ) — FC-03: reload-ul la 1–5 s după primul paint pierdea coșul. (4) `OrderTracker` cunoaște `closed` (Plan 2) ca terminal + etichetă „Închisă"; cardul de loyalty nu se randează pe `rate_limited` (mig 258). (5) `useOrders` are guard de anulare pe fetch-ul inițial și pe heartbeat (DS-4: comenzile lui A sub titlul lui B la comutare); `ManualOrderSheet` citește `error` pe toate query-urile (DS-5). (6) `PaymentMethod` include `card_online` (ambele definiții — orders.ts + constants.ts — ținute sincron); ReportsTab (StatCard/CSV/PDF) + AnalyticsTab au bucket-ul „Card online" (CA-02/MF-12).
- **Enqueue-ul fiscal acoperă și INSERT** (mig 259, lanț 030→259): `enqueue_fiscal_receipt_trg` e `after insert OR update of status` (gate-ul mig 124 acoperea deja INSERT; enqueue-ul nu — un INSERT direct `status='paid'` pe Plan 3 dădea bani fără bon, TĂCUT). Guard-ul din funcție e TG_OP-safe (pe INSERT, OLD nu există — referirea lui aruncă la runtime); orice recreare păstrează TG_OP + skip-ul fără bridge + idempotența pe `pending/sent/success`. Pe INSERT-direct-paid payload-ul pică (itemii nu există încă) → rând `status='error'` VIZIBIL în BridgeTab (retry-ul regenerează payload-ul). Teste FP1–FP4: `tests/sql/fiscal_enqueue_parity_assertions.sql`. **Eșecul AMBIGUU FiscalNet poartă marker** (bridge/lib/fiscalnet.js, oglinda politicii Oblio mig 218): `RESPONSE_TIMEOUT` pe file-mode / abort post-POST / eroare la citirea răspunsului → `errorInfo` prefixat cu `POSIBIL DUPLICAT — verifică banda casei...` + flag `ambiguous`; refuzurile PRE-conectare (ECONNREFUSED/DNS/bad port) NU sunt ambigue (retry sigur). BridgeTab cere confirmare explicită la retrimiterea bonurilor marcate (`handleRetry(id, error_info)`) — un retry orb pe ambiguu = bon fiscal DUBLU real (bandă + Z + ANAF). Testele bridge rulează în CI (test.yml, job „Bridge tests"). **Funcțiile Netlify de BANI au teste unitare** în `tests/functions/` (stripe-webhook, table-payment, oblio-generator; harness zero-deps `helpers/mocks.js` — fake-uri supabase-js/stripe prin `Module._load`, handler-ele rulează NESCHIMBATE): rulează local cu `cd tests/functions && npm test` (fără node_modules!) și în CI în același job „Bridge tests". Vitest NU le vede (include-ul lui e `src/**`) — nu muta fișierele în src/.
- **Capcană test gate-de-plan pe plăți: folosește sume PARȚIALE.** Un test care încearcă plata INTEGRALĂ pe un plan fără `fiscal_receipt` e mascat de `trg_orders_paid_fiscal_gate` (mig 124): la tranziția →paid, trigger-ul aruncă ACELAȘI mesaj „Featurea…" ca gate-ul din `add_partial_payment`, deci testul rămâne verde chiar dacă gate-ul din RPC dispare (verificat empiric). MV5 + PS1 plătesc sub total (trigger-ul nu se atinge) și verifică `order_payments` gol după. O metodă nouă de plată cere sweep pe TOATE breakdown-urile per metodă: ReportsTab (StatCard+CSV+PDF), AnalyticsTab (`v_daily_orders`, lanț view 007→022→116→159→232→253→**263** (gate fiscal ca SEMI-JOIN, 43× măsurat — NU per-rând; mig 263 a adăugat `online_revenue` = `card_online` și `other_revenue` = `other`+NULL, APPEND la final), coloane noi DOAR append la final), CashRegisterTab (labels „Plăți pe metodă").
- **Plata online la masă (mig 202/203; lanț de redefiniri begin: 203→209→211, settle: 203→207→211→229; + cancel mig 208)**: `begin_table_payment`/`attach_payment_intent`/`settle_table_payment`/`cancel_table_payment` = SECURITY DEFINER cu EXECUTE **doar service_role** (le cheamă exclusiv funcțiile Netlify) — NU le da grant la authenticated. Orice recreare TREBUIE să păstreze TOATE: gate-urile din begin (plan `online_payments` pro/enterprise + modul opt-in + `stripe_account_id` fără grant UPDATE + **monedă RON-only**, mig 209 `currency_not_supported`), supersede-ul `superseded_intents` + snapshot-ul `order_totals` (mig 211 — un singur intent live per sesiune; rândurile CU intent NU se mută în begin, doar funcția Netlify după cancel-ul Stripe), iar în settle: terminale DOAR `succeeded/canceled` (mig 207 — retry-ul de card settle-ază), skip pe comenzile cu `order_payments` (mig 211, anti dublă-încasare pe parțiale cash) și notarea totalurilor modificate vs snapshot. `card_online` → cod FiscalNet 7 (de confirmat cu EconMedia: `fiscalnet_payment_code`). Suma se calculează DOAR server-side. Teste permanente TP1–TP12: `tests/sql/table_payment_assertions.sql`.
- **Jobul E2E din ci.yml e ERMETIC** (zero secrets): `supabase start` fără auto-migrații → pre-curățare default privileges pe `service_role` (paritate cu prod; altfel asserția 096B pică la replay-from-zero) → migrațiile aplicate cu **psql** (mig 120/121 au meta-comenzi `\set` pe care runner-ul CLI nu le știe) → user E2E prin GoTrue admin → `seed_tinctura_demo.sql` (owner = userul E2E, plan `enterprise`, altfel trigger-ul de limită de membri (mig 131) respinge membership-ul pe free).
- **Deploy pe VPS**: `deploy/server.js` = shim care rulează funcțiile Netlify NESCHIMBATE (`/.netlify/functions/*`, event Lambda-style, corp RAW pt semnătura Stripe) + cron-urile citite din `netlify.toml` (sursă unică, matcher doar minute/oră — fail-fast pe altceva). Bootstrap: `deploy/setup-vps.sh`; deploy continuu: `.github/workflows/deploy-vps.yml` (inert fără secrets `VPS_*`). Runbook: `docs/VPS_RUNBOOK.md`. **Planul de direcție UNIC: `docs/PLAN_0_TO_HERO.md`** (9 aug 2026, ancorat în date de producție) — MASTER_PLAN/PLAN_10/GO_LIVE rămân doar context istoric, superseded.
